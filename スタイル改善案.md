# スタイル改善案

## 1. 背景と現状認識
- 面情報の視覚表現は「塗りつぶし色と境界線がある」とだけ記されており、色をどう定義・保存・操作するかが不明瞭（要件定義書.md:83-90）。
- 選択時の強調表示はシアンの塗りで統一され、穴にも色が乗る既知課題が残っている（要件定義書.md:381-385、現状.md:73-75）。
- プロパティパネルでは面情報のスタイル操作に関する要件がなく（要件定義書.md:386-389）、ユーザーが色を調整する導線が欠如している。
- データ構造やファイル形式ではスタイル情報が定義されておらず（要件定義書.md:694-750）、今後の拡張余地が大きい。

## 2. 要求整理
1. 面情報は「通常表示色」と「選択表示色」のセットを持ち、穴領域には色が乗らない描画とする。
2. 「自動配色」チェックは、システムが当該面の色を変更してよいかどうかを示す許可フラグであり、OFF にしても既存の色はそのまま保持される。
3. 自動配色の発動タイミングは、面同士の隣接関係が変化したときのみに限定する。
4. レイヤーの描画優先順位とスタイル反映ルールを明文化する。
5. 自動配色に使うテーマ（パレット）の選択肢と UI 方針を示す。

## 3. 仕様案

### 3.1 面スタイルの定義
- 各面情報の歴史状態（錨）には以下の属性を持たせる。
  - **通常表示色**: 通常時の塗りを表す色（RGBA）。穴領域には適用しない。
  - **選択表示色**: 当該面が選択されているときの塗り。境界線の強調は既存仕様を継続。
  - **自動配色許可フラグ**: システムが色を自動更新してよい場合に真となる。フラグを変更しても色値はそのまま残る。
  - **使用パレット**: 自動配色を行う際に参照するテーマ名。自動配色を無効化しても選択値は保持する。
  - **任意メタ情報**: いつ自動配色が走ったかなど、運用上の記録に使えるものを想定（必須ではない）。
- これらは将来のデータ構造で表現される前提だが、ここでは概念レベルに留める（具体的なフィールド名は定義しない）。

### 3.2 自動配色の挙動
1. ユーザーが自動配色を許可すると、現在の隣接関係に基づいて即座に色を決定する。
2. 許可状態が続く限り、隣接関係が更新されたタイミングのみで再配色が行われる。
    1. 頂点の移動だけでは隣接関係が更新されないので、再配色しない。
3. 許可を解除するとシステムによる色更新は停止し、色は自動配色で作成された通りのそのままで保持される。ユーザーは手動で通常色・選択色を編集できる。
4. 再び許可した場合のみ、システムが色を更新する可能性が生まれる
    1. 許可しただけでは色は変わらない

### 3.3 自動配色の考え方
- 四色定理に基づき、同一レイヤー内で共有境界を持つ面が同色にならないように色を割り当てる。
- 四色で足りない場合は、選択パレットの基準色を基に明度・彩度をわずかにずらした派生色を決定的に生成して差別化する。
- 選択表示色は通常表示色より明るく鮮やかになるように調整し、可読性を確保する。
- 飛び地や包領は同じ面情報として扱い、面単位で一貫性のある色を用いる。
- 穴（除外領域）は常に透過扱いとし、選択時にも塗りを乗せない。

### 3.4 再配色トリガー
| イベント             | 内容                                                                        |
| -------------------- | --------------------------------------------------------------------------- |
| 共有境界の成立・解消 | 共有頂点や接続の新規作成/削除によって面同士が接するかどうかが変わったとき。 |
| 面の分割             | 面を複数に分け、新しい面が既存面に隣接する場合。                            |
| 面の結合・合邦       | 複数面が一つになり、隣接リストが再構成される場合。                          |
- 時間移動や単純な頂点移動など、隣接判定に影響しない操作では再配色を行わない。

### 3.5 プロパティパネル UI
- 面情報専用の「スタイル」セクションを設け、以下を配置する。
  1. **自動配色を許可する**（チェックボックス）  
     - ON: 「隣接関係が変わったとき自動で塗り分けを更新します」と案内。  
     - OFF: 「現在の色は維持され、システムは変更しません」と案内。
  2. **使用パレット**（ドロップダウン）  
     - 自動配色が ON の間のみ編集可能。テーマ例：クラシック、パステル、ハイコントラストなど。
  3. **通常表示色**（カラーピッカー＋透過スライダー）
  4. **選択表示色**（カラーピッカー＋透過スライダー）  
     - 自動配色 ON の間は値の閲覧のみ可能、OFF で編集解放。
- 「地物または頂点が選択されていません」という既定メッセージは、選択状況に応じてスタイルセクションの有効/無効と連動させる。

### 3.6 レイヤー表示とテーマ設定
- レイヤーは順序番号の小さいものから描画し、上位レイヤーほど後に描画される。これにより、面スタイルはレイヤーの積層順に従って重ね合わされる。
- レイヤーの不透明度と面スタイルの透過度は掛け合わせて最終的な見た目を決める。
- プロジェクト設定には以下を追加する。
  - **新規面の自動配色既定値**（ON/OFF）
  - **既定パレットテーマ**（プロジェクト全体で統一）
  - **カスタムパレット**（任意、複数色のセットを登録可能）

## 4. 影響範囲と確認ポイント
| 項目                 | 影響内容                                                             |
| -------------------- | -------------------------------------------------------------------- |
| 要件定義書 2.1       | 面のスタイル定義、穴の描画処理、レイヤー描画順を追記。               |
| 要件定義書 2.3       | プロパティパネルにスタイル操作を追加、Undo/Redo の単位を整理。       |
| 要件定義書 2.4       | 描画モード切替の仕様を追加。                                         |
| 要件定義書 2.5 / 4.1 | スタイル属性をデータ構造として定義（フィールド名は後続設計で確定）。 |
| 要件定義書 2.6       | プロジェクト設定にスタイル関連項目を追加。                           |
| UI/UX 全般           | サイドバーやレイヤー設定 UI の情報構成を更新。                       |

## 5. オープン課題
1. **共有境界イベント**: 共有頂点機能が未整備のため、隣接更新トリガーの扱いをどう段階的に導入するかを検討する。
2. **テーマ設計**: 具体的な色セット、色覚バリアフリー対応、ユーザー定義パレットの入力制約などを詰める。
3. **穴の描画修正**: 選択色が穴に乗らないよう描画パイプラインを整理する。（既存不具合）
