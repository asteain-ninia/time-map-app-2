# スタイル改善案（2026-03 更新）

## 0. 文書の役割
- この文書は、面情報スタイル改善の「要件」と「実装方針」を分離して整理する。
- 要件は要件定義書へ反映する正本候補とする。
- 実装方針は設計・実装時の判断材料であり、要件本文の代替にしない。

## 1. 要件（仕様）

### 1.1 対象範囲
- 本改善の対象は面情報（Polygon）のスタイル管理とする。
- 点情報・線情報のスタイル拡張は本改善の対象外とし、別チケットで扱う。

### 1.2 面情報スタイル項目
- 各歴史の錨は、以下のスタイル情報を保持できること。
- 通常塗り色（通常表示時の面塗り）
- 選択塗り色（選択時の面塗り）
- 境界線色
- 自動配色許可フラグ
- 使用パレットID
- 穴領域は常に透過で描画し、通常時・選択時とも塗りを適用しないこと。

### 1.3 自動配色の仕様
- 自動配色許可フラグを ON にした時点で、現時点の隣接関係に基づいて配色を一度実行する。
- ON の間は、隣接関係が変化したタイミングでのみ再配色を行う。
- OFF に変更した場合、システムによる配色更新を停止し、直前の色を保持する。
- ON/OFF の切り替え自体は、既存の色値を破棄しない。

### 1.4 再配色トリガー
- 再配色トリガーは以下に限定する。
- 共有境界の成立・解消
- 面の分割
- 面の結合
- 面の追加・削除で隣接グラフが変化した場合
- 時間移動、単純な頂点移動、表示/非表示切替のみでは再配色しない。

### 1.5 UI要件（プロパティパネル）
- 単一の面情報を編集対象にしたとき、プロパティパネルに「スタイル」セクションを表示する。
- スタイルセクションは以下を提供する。
- 自動配色許可（チェックボックス）
- 使用パレット（選択UI）
- 通常塗り色（色入力）
- 選択塗り色（色入力）
- 境界線色（色入力）
- 自動配色 ON の間は、手動色入力を無効化して値は表示のみとする。

### 1.6 プロジェクト設定要件
- プロジェクト設定に以下を追加する。
- 新規面の自動配色既定値
- 既定パレットID
- 利用可能パレット一覧

### 1.7 保存要件
- スタイル情報は FeatureAnchor の属性領域へ保存し、履歴（Undo/Redo）と同一単位で扱う。
- レイヤーは引き続きスタイルを保持せず、可視性・順序・不透明度のみを責務とする。

## 2. 要件定義書への反映先
- `2.3 オブジェクト編集機能` に面情報スタイル編集 UI を追記する。
- `2.4 注釈・説明機能` に色情報の定義と挙動（ON/OFF・保持）を追記する。
- `2.6 プロジェクト設定の編集` にパレット関連設定を追記する。
- `4.1 基本データ構造` に FeatureAnchor 属性内のスタイル構造を追記する。
- `4.2 保存ファイル形式` の JSON 例にスタイル属性を反映する。

## 3. 実装方針（非要件）
- 配色決定ロジックはアプリケーション層の専用サービスへ集約する。
- 再配色は「隣接グラフ更新イベント」を入力にしたユースケース経由で実行する。
- 描画側は保存済みスタイルを利用し、暗黙の固定色へフォールバックしない。
- 既存 `styleOverrides` は移行期間のみ互換入力として扱い、正規保存形式へ統一する。

## 4. 未決事項
- パレットの初期セット（クラシック、パステル、ハイコントラスト）の具体色。
- 色覚バリアフリー配色の採用基準。
- 既存データに `styleOverrides` しかない場合の移行ポリシー（読み替え時点、保存時点、明示変換のどれで確定するか）。
