# 時空地図アプリケーション - 開発ガイド

## 重要な注意事項

- 余計な変更は行わないでください。
- 指示されていない変更をしないでください。
- 変更箇所を最小限にとどめてください。
- 既存のコメントは温存してください。
- コメントを記載するときは、本質的に必要な内容を書いてください。
  - 不具合の修正等、一時的な情報を含めないでください。具体的には、"// 修正 :～ "等の書き方をしないでください。
    - "//ここを修正"もだめです。"//--修正ここから--"に類するものもだめです
- 論ずるときは、根拠となるソースコードを直接引用してください。
- コードの修正時は、「～も同様に～」といった省略は**一切禁止**します。
- 不具合修正時は、**必ず根本原因をなんとかしてください。場当たり的な対応をしないでください。**
- まずは原因の解析に注力してください。
- 実装上の失敗は開発ガイドに保存していってください。
- 実装のやり残しは `現状.md`に追記していってください。
- 要件定義書は、**仕様本文（完全日本語）**と**内部実装補遺**を分離して記述してください。
  - 仕様本文では、英語識別子・内部キー名・クラス名・コード値（例: territory など）を直接記載しないでください。
  - 実装詳細（データ項目名、保存キー、内部クラス、ユースケース名）は内部実装補遺に集約してください。
  - 仕様本文から実装詳細を参照する場合は、識別子を本文に書かず、章番号で参照してください。

## 1. アーキテクチャ概要

時空地図アプリケーション（time-map-app）は、ドメイン駆動設計（DDD）の原則に基づいて構築されます。アプリケーションは、以下の主要層に分割されています：

### 1.1 ドメイン層

アプリケーションの中核となるビジネスロジックを含みます。

- **エンティティ**:
  - `Vertex`: 地図上の点を表す不変オブジェクト
  - `Feature`: すべての地理オブジェクトの基底クラス
  - `Point`, `Line`, `Polygon`: 具体的な地理的地物を表すクラス
  - `Layer`: レイヤー情報を表すクラス

- **値オブジェクト**:
  - `Coordinate`: 座標値を表す不変オブジェクト
  - `TimePoint`: 時間値を表す不変オブジェクト
  - `Property`: 特定の時点での属性値を表す不変オブジェクト

- **ドメインサービス**:
  - `GeometryService`: 幾何学計算の機能を提供
  - `TimeService`: 時間に関する操作と計算を行う
  - `LayerService`: レイヤー間の関係管理を担当

- **集約ルート**:
  - `World`: すべての地理情報とレイヤー構造を含む集約

- **リポジトリインターフェース**:
  - `WorldRepository`: データの保存・読み込みの抽象化

### 1.2 アプリケーション層

ユーザー操作をドメインロジックに変換し、結果を表示層に返す役割を担います。

- **ユースケース**:
  - `EditFeatureUseCase`: 地理オブジェクトの編集を処理するファザード
    - `AddFeatureUseCase`: 地理オブジェクトの追加を処理
    - `UpdateFeatureUseCase`: 地理オブジェクトの更新を処理
    - `DeleteFeatureUseCase`: 地理オブジェクトの削除を処理
    - `VertexEditUseCase`: 頂点編集を処理
    - `PolygonEditService`: ポリゴンのリング構造編集を担当するサービス
  - `NavigateTimeUseCase`: 時間移動を処理
  - `ManageLayersUseCase`: レイヤー管理を処理

- **コマンド**:
  - `AddFeatureCommand`: 新しい地理オブジェクトの追加
  - `UpdateFeatureCommand`: 既存の地理オブジェクトの更新
  - `DeleteFeatureCommand`: 地理オブジェクトの削除
  - `MoveTimeCommand`: 時間位置の変更

- **クエリ**:
  - `GetFeaturesAtTimeQuery`: 特定時点の地理オブジェクトを取得
  - `GetLayerStructureQuery`: レイヤー構造を取得

- **イベント**:
  - `FeatureAddedEvent`: 地理オブジェクト追加イベント
  - `TimeChangedEvent`: 時間変更イベント
  - `LayerVisibilityChangedEvent`: レイヤー可視性変更イベント

### 1.3 インフラストラクチャ層

外部リソースへのアクセスやフレームワーク統合などの技術的詳細を処理します。

- **リポジトリ実装**:
  - `JSONWorldRepository`: JSONファイルベースのリポジトリ実装

- **永続化**:
  - `FileSystem`: ファイルシステム操作の抽象化
  - `JSONSerializer`: JSON形式でのデータシリアライズ/デシリアライズ

- **レンダリング**:
  - `SVGRenderer`: SVGベースの地図レンダリング
  - `D3Integration`: D3.jsライブラリとの統合
  - `ViewportManager`: 地図表示領域（ズーム、パン）の管理

- **外部サービス**:
  - `Logger`: ロギングサービス
  - `ConfigManager`: 設定管理

### 1.4 プレゼンテーション層

ユーザーインターフェースとユーザーとの対話を担当します。

- **ビュー**:
  - `MapView`: メインマップ表示
  - `TimelineView`: タイムライン表示
  - `ToolbarView`: ツールバー表示
  - `SidebarView`: サイドバー表示

- **ビューモデル**:
  - `MapViewModel`: マップビューのデータと状態管理
  - `TimelineViewModel`: タイムラインビューのデータと状態管理
  - `EditingViewModel`: 編集関連の状態管理

- **コントローラ**:
  - `MapController`: マップ操作処理
  - `TimelineController`: タイムライン操作処理
  - `ToolController`: 編集ツール操作処理

## 2. ファイル構造

プロジェクトのファイル構造は、アーキテクチャの層と責務に沿って組織されます。

time-map-app
│  .clinerules
│  .gitattributes
│  .gitignore
│  electron-main.js
│  LICENSE
│  package-lock.json
│  package.json
│  preload.js
│  README.md
│  webpack.config.js
│  world.json
│  現状.md
│  要件定義書.md
│  開発ガイド.md
│  
├─assets
│  └─maps
│          base-map.svg
│          
├─backups
│      
├─dist
├─public
│      index.html
│      
└─src
    ├─application
    │  │  WorldRepository.js
    │  │  
    │  ├─services
    │  │  │  IdGenerationService.js
    │  │  │  IPolygonEditService.js
    │  │  │  PolygonEditService.js
    │  │  │  HistoryService.js
    │  │  │ 
    │  │  └─history
    │  │              HistoryStackManager.js
    │  │              HistorySerializer.js
    │  │              OperationEngine.js
    │  │      
    │  └─usecases
    │      │  EditFeatureUseCase.js
    │      │  ManageLayersUseCase.js
    │      │  NavigateTimeUseCase.js
    │      │  UpdateProjectSettingsUseCase.js
    │      │  
    │      └─feature
    │              AddFeatureUseCase.js
    │              DeleteFeatureUseCase.js
    │              UpdateFeatureUseCase.js
    │              VertexEditUseCase.js
    │              
    ├─domain
    │  ├─entities
    │  │      Feature.js
    │  │      Layer.js
    │  │      Line.js
    │  │      Point.js
    │  │      Polygon.js
    │  │      Vertex.js
    │  │      
    │  ├─services
    │  │      GeometryService.js
    │  │      LayerService.js
    │  │      TimeService.js
    │  │      
    │  └─value-objects
    │          Coordinate.js
    │          Property.js
    │          TimePoint.js
    │          
    ├─infrastructure
    │  ├─persistence
    │  │      FileSystem.js
    │  │      JSONSerializer.js
    │  │      JSONWorldRepository.js
    │  │      
    │  ├─rendering
    │  │      SVGRenderer.js
    │  │      ViewportManager.js
    │  │      
    │  └─services
    │          ConfigManager.js
    │          Logger.js
    │          
    ├─main
    │      Config.js
    │      DependencyInjection.js
    │      index.js
    │      
    └─presentation
        │  EventBus.js
        │  
        ├─controllers
        │      MapController.js
        │      TimelineController.js
        │      ToolController.js
        │      
        ├─view-models
        │      EditingViewModel.js
        │      MapViewModel.js
        │      TimelineViewModel.js
        │      
        └─views
            │  MapView.js
            │  SidebarView.js
            │  TimelineView.js
            │  ToolbarView.js
            │  
            └─map
                    MapViewEventHandler.js
                    MapViewInteractionLogic.js
                    MapViewRendererHelper.js

## 3. 実装ガイドライン

### 3.1 一般的なコーディング規約

- **JavaScript標準**: ES2022+の機能を活用する
- **型チェック**: TypeScriptまたはJSDocを使用した型注釈を使用
- **コードスタイル**:
  - インデントは2スペース
  - セミコロンを使用
  - 命名規則:
    - クラス名: PascalCase (`FeatureRenderer`)
    - 変数/関数: camelCase (`getCurrentYear`)
    - 定数: UPPER_SNAKE_CASE (`MAX_ZOOM_LEVEL`)
    - プライベートメンバー: 先頭にアンダースコア (`_privateMethod`)
  - 関数は1つのことだけを行い、30行以下に抑える
  - ネストを3レベル以下に抑える

- **コメント**:
  - クラスや重要な関数はJSDocコメントで説明
  - 複雑なロジックには適切な説明コメントを追加
  - コメントは日本語で記述する

### 3.2 ドメイン層の実装ガイドライン

- **不変性 (Immutability)**: エンティティと値オブジェクトは不変として設計します。状態を変更する操作は、元のオブジェクトを変更するのではなく、新しいインスタンスを生成して返します。これにより、状態の予測可能性を高め、副作用を減らします。
- **値オブジェクトの等価性**: 値オブジェクトは、その属性値によって等価性が判断されます。`equals()` メソッドを実装し、属性値を比較するようにします。
- **ドメインサービス**: 特定のエンティティに属さない、ドメイン横断的なロジック（例: 幾何学計算）はドメインサービスとして実装します。ドメインサービスはステートレスに保ちます。
- **関心の分離**: ドメインロジック（ビジネスルール、計算）はドメイン層に閉じ込め、他の層（特にインフラストラクチャ層やプレゼンテーション層）に漏れ出さないようにします。
- **集約とリポジトリ**: 集約ルート（例: `World`）を定義し、その整合性を保つように設計します。集約間の参照はIDによって行い、リポジトリは集約ルート単位でデータの永続化を扱います。

### 3.3 アプリケーション層の実装ガイドライン

- **CQRS原則**: 操作（コマンド）とデータ取得（クエリ）を明確に分離します。コマンドは状態変更のみを行い、値を返しません（または操作結果を示す最小限の情報のみ）。クエリは状態を変更せず、データを返します。
- **ユースケース**: ユーザーの意図する操作（例: 地物追加、時間移動）をユースケースとして定義します。各ユースケースは特定のタスクを実行し、ドメインオブジェクトやサービス、リポジトリを調整します。
- **コマンド/クエリバス**: コマンドとクエリのディスパッチを一元管理するバス（オプション）を導入することで、コンポーネント間の結合度を下げることができます。
- **トランザクション管理**: 複数のステップを含む操作（例: 地物追加と関連頂点保存）は、アプリケーション層でトランザクション境界を管理し、操作の原子性を保証します。
- **イベント駆動**: 状態変更（例: 地物追加完了）が発生した場合、対応するイベントを発行します。他のコンポーネント（例: ViewModel）はイベントを購読し、UI更新などの副作用を処理します。

### 3.4 インフラストラクチャ層の実装ガイドライン

- **依存性隠蔽**: 外部ライブラリやフレームワーク、データストレージなど、技術的な詳細への依存はインフラストラクチャ層に閉じ込め、アプリケーション層やドメイン層から隠蔽します。
- **アダプタパターン**: 外部依存性を抽象化するためのアダプタ（例: `FileSystem`）を定義し、アプリケーションはインターフェースに依存するようにします。
- **リポジトリ実装**: ドメイン層で定義されたリポジトリインターフェースを実装します。リポジトリは永続化の詳細（例: JSONファイルへの書き込み）を隠蔽し、ドメインオブジェクトの形式でデータを返します。
- **例外変換**: インフラストラクチャ層で発生した低レベルな例外（例: ファイルI/Oエラー）は、より抽象的なアプリケーション固有の例外（例: `PersistenceError`）に変換して上位層に伝播します。
- **設定管理**: 環境設定や外部サービスへの接続情報などは外部化し（例: `ConfigManager`）、アプリケーションコードから分離します。

### 3.5 プレゼンテーション層の実装ガイドライン

- **MVVMパターン**: Model-View-ViewModelパターンを採用し、UI（View）、UIの状態とロジック（ViewModel）、データとビジネスロジック（Model、アプリケーション層/ドメイン層が担当）を分離します。
- **ビューの責務**: ビューはUI要素の表示とユーザー入力の受付に専念します。複雑な表示ロジックや状態管理はViewModelに委譲します。
- **ビューモデルの責務**: ビューの状態（表示データ、UI要素の有効/無効状態など）を保持し、ビューからのユーザー操作（コマンド）を受け取り、アプリケーション層（ユースケース）に伝達します。また、アプリケーション層からのイベントを購読し、状態を更新してビューに通知します。
- **データバインディング**: ビューとビューモデル間の状態同期には、データバインディングの仕組み（フレームワーク機能または手動実装）を利用します。
- **コマンドパターン**: ビューでのユーザー操作（ボタンクリックなど）はコマンドオブジェクトに変換され、ViewModelに渡されます。ViewModelがコマンドを解釈し、適切なユースケースを呼び出します。

### 3.6 テスト戦略

- **単体テスト**: ドメイン層（エンティティ、値オブジェクト、ドメインサービス）とアプリケーション層（ユースケース）のロジックを中心に、個々のクラスやメソッドの機能をテストします。依存関係はモックやスタブを使用して分離します。
- **統合テスト**: 複数のコンポーネント間の連携をテストします。特に、アプリケーション層とインフラストラクチャ層の連携（例: ユースケース -> リポジトリ -> 永続化）や、ドメインサービスとエンティティの連携などを検証します。
- **エンドツーエンドテスト**: ユーザーの視点から、主要な機能シナリオ（例: 地物追加フロー、時間移動と表示更新）全体をテストします。UI操作からデータの永続化までの一連の流れを確認します。

#### テスト実装のコツ

- `npm test` で Vitest が実行される。テストは `tests/` 配下に配置し、ファイル先頭に作成者をコメントで明示する。
- ドメイン層・アプリケーション層のテストは依存を DI で差し替え、Electron 依存のあるクラスは `window` やファイルシステムをモックして Node 環境で動かす。
- 既存の挙動に疑問がある場合でも、まずは現状を再現するテストを追加し、改善案は別ブランチや Issue で整理する。
- Electron の I/O を伴う統合テストは重いので、基本はドメイン→アプリケーション→インフラ→プレゼンテーションの順で段階的にテストを整備し、必要なシナリオだけを E2E に昇格させる。
- `GeometryService` などログ出力を行う既存クラスでは、テスト中の警告ログを許容しつつ、失敗と混同しないようテスト名で目的を明確にする。

## 4. 重要な機能の実装ガイド

### 4.1 共有頂点システムの実装

#### 概念
- **近接検出**: ユーザーが頂点を移動させた際、他の頂点との画面上の距離を計算し、閾値以下であれば共有候補とするアルゴリズムが必要です。
- **共有グループ**: 共有される頂点群とその代表座標を管理するデータ構造。主頂点（例: 最も古いID）を基準に座標を保持します。
- **連動更新**: 共有頂点が移動された場合、グループ内の全ての頂点の座標を更新し、関連する地物の形状も更新するロジックが必要です。
- **共有解除**: 特定の頂点を共有グループから解除し、グループ内の頂点が1つになった場合はグループ自体を解散させる処理が必要です。

#### 実装ポイント
- 頂点移動操作時、近接検出サービスを呼び出して共有化を試みる。
- 共有頂点ドラッグ時には、`VertexEditUseCase` が共有グループ情報を参照し、関連する全頂点の移動コマンドを発行する。
- コンテキストメニューなどから共有解除操作をトリガーし、`VertexEditUseCase` 内で解除処理と関連地物の更新を行う。

- **教訓**: 共有頂点解除のように頂点IDを差し替える処理では、検証前に `world.features` を書き換えないこと。失敗時は追加した頂点と地物の両方を必ずロールバックする。`VertexEditUseCase.unlinkSharedVertex` では自己交差エラー時に地物側の頂点IDだけが新頂点のまま残り、世界データが破損した。
- **教訓**: 共有頂点化で残す頂点をIDの古さだけで決めると、ドラッグで保持していない頂点の座標が失われ、スナップ結果が反映されない。共有化の保持頂点は操作コンテキスト（ドラッグ対象かどうか）に基づいて決定する。
- **教訓**: リング階層の削除では `ringType` ごとの許容親を尊重する。`src/domain/entities/Polygon.js:248-275` の旧実装は親領土リング削除時に穴リングを `parentId: null` のまま残し、`src/infrastructure/rendering/SVGRenderer.js:664-682` のラベル描画で対象外になった。直下の穴はカスケード削除し、領土リングのみを親の親へ付け替える。
- **教訓**: レイヤー階層検証を伴う処理では、結果が確定するまで `world.layers` を破壊的に更新しない。`ManageLayersUseCase.addLayer` と `reorderLayer` が検証前に `world.layers` を書き換え、失敗時に追加済みレイヤーや並べ替え結果が残留して `JSONWorldRepository` のインメモリキャッシュ経由で世界全体へ汚染が伝播した。検証用の仮配列を作成し、成功が確認できた段階でのみ世界データへ反映する。
- **教訓**: 永続化処理が失敗する可能性を常に想定し、成功が確定するまで共有キャッシュの `world` オブジェクトを破壊的に更新しない。旧 `ManageLayersUseCase.addLayer` / `updateLayer` / `deleteLayer` / `reorderLayer`（`src/application/usecases/ManageLayersUseCase.js`）は `world.layers` を先に書き換えた状態で `JSONWorldRepository.saveWorld` を呼び出し、`FileSystem.writeFile`（`src/infrastructure/persistence/FileSystem.js`）で I/O 例外が発生すると `JSONWorldRepository.getWorld` が返すキャッシュが汚染された。現在は `_buildWorldWithLayers` と `_replaceLayersInPlace` で保存用のコピーを構築し、`saveWorld` 成功後にのみキャッシュへ反映することで、失敗時に世界データを旧状態に保つ。
- **教訓**: レイヤーIDを生成する際は、`new Date().getTime()` などミリ秒精度のソースだけに依存しない。`ManageLayersUseCase.addLayer` の旧実装は同一ミリ秒内で複数レイヤーを追加するとIDが衝突し、削除や更新が意図通りに行えなくなった。ID生成サービスを経由し、既存レイヤーとの重複検査を行ってから確定させる。
### 4.2 レイヤー階層管理の実装

#### 概念
- **階層構造**: レイヤー間の親子関係（`parentId`）と順序（`order`）を定義し、データ構造として保持します。
- **包含関係**: 下位レイヤーの面情報は、必ず上位レイヤーのいずれかの面情報内に存在するというルールを強制します。
- **形状の導出**: 下位領域を持つ面情報の形状は、下位領域の形状の和集合として動的に計算されるべきです（要件定義書参照、現状実装とは異なる可能性あり）。

#### 実装ポイント
- `LayerService` に、レイヤー階層の妥当性（順序の重複なし、親子関係の整合性）を検証するメソッドを実装します。
- 面情報の追加・更新時に、`LayerService` を用いて上位レイヤーとの包含関係を検証するロジックを `UpdateFeatureUseCase` 等に組み込みます。
- 下位領域を持つ面情報の描画時には、`LayerService` を使って下位領域を取得し、それらの形状を結合して描画するロジックを `SVGRenderer` または `MapViewModel` に実装します（ポリゴン結合アルゴリズムが必要）。

### 4.3 時間依存データ管理の実装

#### 概念
- **プロパティ配列**: 各地物は `properties` 配列を持ち、各要素は特定の `TimePoint` から有効になる属性セット (`Property` インスタンス) です。
- **現在値の取得**: 特定の時間点が与えられた場合、`properties` 配列を時間でソートし、その時間点以前で最も新しい `Property` インスタンスを選択することで、その時点での有効な属性を取得します。
- **存在期間**: `Property` インスタンスにオプションの `timeRange`（`startTime`, `endTime`）を持たせることで、地物の存在期間を表現できます。

#### 実装ポイント
- `Feature.getPropertyAt(timePoint)` メソッドで、上記ロジックに基づき特定の時間点での有効な `Property` インスタンスを返すようにします。
- `Feature.existsAt(timePoint)` メソッドで、`getPropertyAt` の結果が `null` でないか、および `timeRange` の制約を満たすかで存在判定を行います。
- 時間スライダーが変更された際に `TimeChangedEvent` を発行し、`MapViewModel` がこれを購読して `loadFeaturesForCurrentTime()` を呼び出し、表示地物を更新するフローを確立します。

### 4.4 領域排他性と衝突判定の実装

#### 概念
- **排他性ルール**: 同一レイヤー内の面情報は重ならないというドメインルール。
- **衝突検出**: 2つのポリゴン（リング構造を考慮）が重なっているか（境界接触を含むか含まないか）を判定するアルゴリズム。
- **エッジ滑り**: 頂点移動時に他のポリゴンの境界に衝突した場合、移動を境界線に沿わせる処理。

#### 実装ポイント
- `GeometryService` に、リング構造を考慮したポリゴン間の重なり判定 (`doPolygonsOverlap`)、線分交差判定 (`doLineSegmentsIntersect`)、点の内外判定 (`isPointInPolygon`) などのメソッドを実装します。
- `UpdateFeatureUseCase` (特に頂点移動処理) 内で、移動後の形状が同一レイヤーの他のポリゴンと衝突しないかを `GeometryService` を用いて検証します。
- 衝突が発生する場合、エッジ滑りロジックを適用します。これは、衝突対象ポリゴンのエッジに移動点を投影 (`GeometryService.projectPointToEdge`) し、ドラッグ方向に最も近いエッジ上の点を新しい位置とする処理を含みます。

### 4.5 SVGレンダリングの実装について

  SVGRenderer では、ビューポートの変換に `viewBox` 属性を使用します。これにより以下の利点があります：

  - SVG標準の変換機能を活用し、個別要素の座標計算が簡略化される
  - パフォーマンスの向上（要素ごとの座標再計算が不要）
  - ズーム・パン操作の正確性向上

  ViewportManager からの更新情報は SVGRenderer の render メソッドで viewBox 属性に反映され、SVG内の座標系全体が自動的に変換されます。

## 5. デバッグと最適化

### 5.1 パフォーマンス最適化

- **レンダリング最適化**:
  - **カリング**: 表示領域外のオブジェクトは描画処理から除外します (`SVGRenderer` 内でビューポート座標と比較)。
  - **LOD (Level of Detail)**: ズームレベルに応じて地物の表示を簡略化します。遠景では頂点を間引いたり、小さな地物を省略したりします。
  - **バッチ処理**: 複数の描画命令をまとめて処理することで、DOM操作のオーバーヘッドを削減します（必要な場合）。
- **データ構造最適化**:
  - **空間インデックス**: 地物の検索（例: クリック位置に近い地物を見つける）を高速化するために、四分木 (Quadtree) や R木 (R-tree) などの空間インデックスの導入を検討します。
  - **メモリ管理**: 大量のデータを扱う場合、不要になったオブジェクト参照を適切に解除し、ガベージコレクションを促進します。
- **イベント処理最適化**:
  - **スロットリング/デバウンシング**: マウス移動やウィンドウリサイズのような高頻度イベントは、処理回数を制限（スロットリング）するか、イベント発生が止まってから処理（デバウンシング）します。

### 5.2 ロギングと診断

- **ログレベルの活用**: 開発時（デバッグレベル）と本番時（情報レベルや警告レベル）でログレベルを切り替えられるようにします。
- **構造化ログ**: ログメッセージにコンテキスト情報（クラス名、メソッド名、関連IDなど）を含め、解析しやすくします。
- **パフォーマンス計測**: 主要な処理（データロード、レンダリング、計算）の実行時間を計測し、ボトルネックを特定します。ブラウザの開発者ツールや `performance.now()` を活用します。
- **エラー監視**: エラー発生時に詳細な情報（エラーメッセージ、スタックトレース、関連データ）をログに記録し、原因究明を容易にします。

### 5.3 エラー処理戦略

- **エラー分類**: ドメインルール違反、入力検証エラー、外部リソース（ファイルシステム等）エラー、予期せぬエラーなどを区別し、異なるハンドリングを行います。
- **早期リターンとガード節**: 関数の冒頭で不正な引数や状態をチェックし、早期に処理を中断することで、後続処理での複雑なエラーハンドリングを減らします。
- **ユーザーへのフィードバック**: エラー発生時、技術的な詳細を隠蔽しつつ、ユーザーに状況と可能な対処法（例: 再試行、保存して終了）を分かりやすく伝えます。
- **回復処理**: 可能な場合は、エラー発生前の状態に復元する（例: トランザクションのロールバック、アンドゥスタックの利用）か、安全な状態（例: データの自動保存）に移行します。

## 6. 開発フロー

### 6.1 開発プロセス

1.  **要件分析**: ユーザーストーリー、ユースケース、受け入れ基準を定義。
2.  **設計**: ドメインモデル、アーキテクチャ、インターフェースを設計。
3.  **実装**: TDDを適用し、継続的インテグレーションを実施。コードレビューを行う。
4.  **テスト**: 単体、統合、E2Eテスト、ユーザビリティテストを実施。
5.  **デプロイ**: 継続的デプロイメント、バージョン管理、リリースノート作成。
6.  **ドキュメント改善**: バグ修正や仕様変更時にドキュメントを更新し、最新の状態を維持する。

### 6.2 Git ワークフロー

- **ブランチ戦略**: Git Flow または GitHub Flow に類似した戦略を採用。
  - `main`: 安定版
  - `develop`: 開発統合用
  - `feature/*`: 機能開発
  - `bugfix/*`: バグ修正
  - `release/*`: リリース準備
- **コミットメッセージ**: Conventional Commits 規約に従う (`<type>(<scope>): <subject>`)。
  - 現状.mdの更新のみが含マれる場合、コミットメッセージは`docs: 不具合リストの更新`に統一する。
  - コミットメッセージには全文**日本語を用いる**。ただし、`<type>`については前例に準じ英単語を使う。

### 6.3 リリースサイクル

- **バージョニング**: セマンティックバージョニング (`Major.Minor.Patch`) を採用。
- **リリースプロセス**: リリースブランチでの最終テスト、バージョン更新、タグ付け、リリースノート作成を経てリリース。

## 8. 開発上の注意点とよくある落とし穴

開発中に発生した問題とその教訓を記録し、同様の誤りを防ぐためのガイドラインとします。

### 8.1 状態管理と更新ロジック

#### 8.1.1 時間変更など、状態変化に依存する他の状態の更新漏れ
*   **問題**: タイムライン操作で時間が変更された際、以前の時間で選択されていた地物が新しい時間では存在しない場合でも、その選択状態（ハイライト等）が解除されずに残った。
*   **原因**: `MapViewModel` で時間変更イベントを処理する際、表示地物リストは更新していたが、選択中の地物(`_activeFeature`)が新しい時間点でも存在するか(`existsAt(currentTime)`)を検証し、存在しない場合に選択を解除する処理が欠けていた。
*   **教訓**:
    *   状態間の依存関係（例: 時間と選択状態）を認識し、ある状態が変化したら、それに依存する他の状態も検証・更新するロジックを実装する。
    *   イベントハンドラでは、イベントが影響しうる全ての状態変数を考慮し、更新漏れがないか確認する。
    *   「選択中の地物は現在の時間で存在しなければならない」などの暗黙的なルールはコード上で明示的にチェック・強制する。

#### 8.1.2 派生状態に基づく操作制約の欠如 (例: 非表示要素の選択)
*   **問題**: ユーザーがUI上で直接見えていない、あるいは操作対象となるべきでない要素（例: タイムラインで非表示になっている過去/未来の地物の頂点、非表示レイヤー上の要素）が、選択や編集の対象になってしまう。
*   **原因**: 要素を選択・操作するロジック（特に`MapView._findClosestVertex`）が、アプリケーションの現在のコンテキスト（表示中の時間、表示レイヤー、フィルター状態など）を考慮せず、データソース全体（例: `world.vertices` 全て）を対象としていた。表示状態を管理するViewModel (`MapViewModel._features` 等）と、操作対象を決定するロジックが連携していなかった。
*   **教訓**:
    *   ユーザー操作の対象となる要素は、原則として**現在UI上に表示されている要素**に限定するべきである。
    *   要素を検索・選択・操作する際は、ViewModelが管理する**現在の表示状態**（フィルタリングされたリストなど）を基準にする。データソース全体を直接検索しない。
    *   状態管理（ViewModel）は、現在のコンテキストに基づいて操作可能な要素リストを提供する責務も持つ。
    *   Viewでの近接検索 (`_findClosestVertex`等) は、ViewModelから取得した**表示中の地物** (`viewModel.getFeatures()`) が参照する頂点IDセットなど、**表示中の要素リスト**を対象に行う。
    *   ViewModelの選択メソッド (`selectVertex`等) にも、対象要素が**現在表示中であるか**を検証するガード節を追加することが望ましい。

#### 8.1.3 Undo/Redoスタック操作の副作用管理不備
*   **問題**: Redo操作を1回実行すると、残りのRedoスタックがクリアされ、複数回のRedoが実行できなくなる。
*   **原因**: `HistoryService.redo()`内で、Redoされた操作をアンドゥスタックに戻すために `HistoryStackManager.pushUndo()` を呼び出していた。この `pushUndo()` メソッドは、新しい編集操作が行われたと解釈し、副作用として常にリドゥスタックをクリア (`clearRedoStack()`) していた。
*   **教訓**:
    *   副作用を持つメソッド（例: `pushUndo` が `clearRedoStack` を呼ぶ）を設計・使用する際は、その副作用が全ての呼び出しコンテキストで適切かどうかを慎重に検討する。
    *   特定のコンテキスト（例: Redo操作からの `pushUndo`）で副作用を抑制したい場合は、フラグ引数を追加するか、副作用のない専用メソッドを提供するなどの対策を講じる。
    *   Undo/Redoのような状態遷移が複雑な機能では、各操作がスタックや他の状態に与える影響を正確に追跡し、意図しない状態変更が発生しないように注意深く設計する。

#### 8.1.4 分離されたコンポーネント間での状態同期漏れ
*   **問題**: 一部のコンポーネント（例: `OperationEngine`）が共有データソース（例: `WorldRepository`のキャッシュ）を直接更新した後、他のコンポーネント（例: `MapViewModel`）がその変更を検知できず、古いデータを参照し続けてしまい、UIの表示不整合やエラーを引き起こした。Undo操作後に再選択してもハイライトが更新されない問題がこの典型例であった。
*   **原因**: データソースを更新したコンポーネントが、その変更をシステム全体に通知する仕組み（イベント発行など）が欠落していた。また、データソースに依存するコンポーネントが、自身の内部状態を能動的に再同期する仕組みを持っていなかった。
*   **教訓**:
    *   あるコンポーネントが共有リソースの状態を更新した場合、そのリソースに依存する他の全てのコンポーネントが変更を検知し、自身の状態を同期させる仕組みが不可欠である。
    *   イベントバスを通じて汎用的な状態変更イベント（例: `WorldUpdated`）を発行し、各コンポーネントがそれを購読して自身のデータをリフレッシュする、というパターンは、コンポーネント間の結合度を下げつつ状態の一貫性を保つのに有効である。
    *   ViewModelは、自身の内部に保持するデータ（例: `this._world`）が、必ずしも常にデータソースの最新状態と一致するとは限らないことを前提に設計し、適切なタイミングで再同期するロジックを持つべきである。

#### 8.1.5 プロジェクト設定とアプリ設定の責務混在
*   **問題**: ズーム最小/最大がプロジェクト設定として保存・編集されるべきなのに、アプリ設定扱いで固定値が適用され、プロジェクト設定画面から変更できなかった。
*   **原因**: `world.metadata.settings` に保持すべき値を `Config`/`ConfigManager` 側へ固定し、UI→保存→反映の経路が分断された。
*   **教訓**:
    *   プロジェクト固有の設定は `world.metadata.settings` に集約し、編集UI・永続化・適用先の3点が同じ情報源を参照するように設計する。
    *   アプリ設定とプロジェクト設定の境界を先に定義し、保存先と反映先を一貫させる。

### 8.2 UIインタラクションとユーザー操作フロー

#### 8.2.1 UI操作フローの完了トリガー欠落 (例: 確定ボタンの未実装)
*   **問題**: 地物追加モードで頂点を配置した後、形状を確定してオブジェクト追加を完了させるUI操作（確定ボタンやダブルクリック等）が実装されていなかった。
*   **原因**: 要件にはあったがUI (`ToolbarView`, `MapView`) に確定ボタン等が実装されておらず、ViewModelの確定メソッド (`confirmAddFeature`) が呼び出される経路がなかった。
*   **教訓**: UIインタラクションのフロー（開始、途中操作、**完了/キャンセル**）を設計・実装する際は、必ず「完了」させるためのトリガーを明確に定義し、実装する。ViewModelにメソッドがあってもUIからの呼び出しがなければ機能しない。

#### 8.2.2 排他的なイベント処理による意図しない動作 (例: 頂点クリック時の地物選択阻害)
*   **問題**: 編集モードで頂点をクリックすると頂点のみが選択され、属する地物が選択されなかった。
*   **原因**: `MapView._onMouseDown` で、頂点が見つかった場合に処理を終了し、地物選択ロジックを実行していなかった。頂点が見つからない場合にのみ地物選択が試みられていた。
*   **教訓**: UIイベント処理では、ユーザーの期待する動作（例: オブジェクトの一部をクリックしたら全体が選択される）を考慮する。排他的な条件分岐が他の重要な処理をスキップさせていないか確認する。選択対象の優先順位と、同時選択・排他的選択のどちらが適切かを明確に定義する。

#### 8.2.3 オーバーレイ要素上のモーダルUIでのイベント処理競合
*   **問題**: 地図表示エリアの上にモーダルダイアログ（プロパティ入力など）を表示した際、ダイアログ内の入力要素がフォーカスを失ったり、クリックに反応しなくなったりした。
*   **原因**: `MapView` のイベントリスナー（例:`_onMouseDown`）が、イベント発生源 (`event.target`) がモーダルダイアログ上にあるかどうかをチェックせずに、常に地図操作（パン開始、要素選択など）を実行していた。これにより、ダイアログ上の操作が意図せず地図操作をトリガーし、フォーカス喪失やイベント阻害が発生した。
*   **教訓**: オーバーレイ要素の上に別のUI要素（ダイアログ、メニュー等）を表示する場合、オーバーレイのイベントリスナー内で `event.target` を検証し、イベントが上位UI要素内で発生した場合は、下位のオーバーレイ（地図）側の処理を実行しないようにする。イベントの伝播制御 (`stopPropagation()`) だけでは不十分な場合があり、リスナーの処理自体を条件分岐させる必要がある。要素が特定の親要素（例: ダイアログのコンテナ）に含まれるかは `element.closest('.dialog-class-name')` などで判定できる。


#### 8.2.4 選択ハイライトで穴が塗り潰される
*   **問題**: 穴を含むポリゴンを選択した際、選択オーバーレイが穴領域まで塗り潰し、視覚上ポリゴン全体が埋まってしまった。穴の中に存在する飛び地も強調表示されず、地物の境界が分からなくなった。
*   **原因**: `MapViewRendererHelper.renderSelection`（`src/presentation/views/map/MapViewRendererHelper.js:100-167`）がリングごとに `drawLine` を呼び出して塗り要素を作成していたため、穴リングの座標を同じ `path` に含められず、`fill-rule="evenodd"` が機能しなかった。恒常描画 (`SVGRenderer._renderPolygon`) ではリング階層を考慮して複合パスを生成しているが、選択オーバーレイ側で同じロジックを共有していなかった。
*   **教訓**:
    *   穴や飛び地を持つポリゴンの塗り処理では、領土リングとその穴リングを同じ `path` のサブパスとして定義し、`fill-rule="evenodd"` を適用しなければならない。リング単位で別 `path` を作ると穴が機能しない。
    *   本体描画とオーバーレイ描画で数学的に同じ図形を扱う場合、リング階層の解釈と座標列生成ロジックを共有する。別実装にすると、穴・飛び地など複雑なケースで乖離が発生する。
    *   補助描画用のレンダラ（`MapViewRendererHelper`）にはリング階層を平坦化して複合パスを生成するユーティリティを用意し、`SVGRenderer` 側にも複数サブパスを扱えるメソッドを提供しておくと、同種の不具合を再発させにくい。

#### 8.2.5 選択状態の分類漏れによる空表示 (例: PropertiesTabView)
*   **問題**: サイドバーのプロパティタブが複数の地物に跨る頂点選択中でも「地物または頂点が選択されていません」と誤表示し、属性編集ができなかった。
*   **原因**: `PropertiesTabView.update`（`src/presentation/views/sidebar/PropertiesTabView.js:51-66`）が `MapViewModel.getSelectionContextFeature()` の戻り値のみで UI 分岐を行い、`MapViewModel.getVertexSelectionOwnerIds()` や `getSelectedVertexIds()` が返す元の選択集合を参照していなかった。頂点選択の所有者が複数に及ぶと `getSelectionContextFeature()` は `null` を返す設計のため、未選択と複数選択が同じ分岐に落ち、仕様で定義された複数選択メッセージが表示されなかった。
*   **教訓**: 複数の選択状態（単一・複数・不明）を扱う UI では、派生済みのヘルパー結果だけに依存せず、生の ID セット（頂点集合、所有者集合、アクティブID）をすべて評価した上で分岐を網羅する。仕様側で列挙した空状態メッセージ（例: 未選択、複数選択、所有者不明）は View の条件分岐と一対一で対応付け、どの状態でもユーザーが次に取るべき操作を把握できるようにする。

#### 8.2.6 設定フォームのスクロール領域不足
*   **問題**: サイドバー内の設定フォームが縦長になった際、保存ボタンが画面外に隠れて操作不能になった。
*   **原因**: `SidebarView` のタブ領域が `overflow: hidden` で、`ProjectSettingsTabView` が `flex: 1` だけに依存して高さが確定していなかったため、スクロール領域が成立せず下部が切り落とされた。
*   **教訓**:
    *   フォームの操作ボタンが下部に配置される UI では、親の高さと `overflow` の責務を明確にし、スクロール領域に `min-height: 0` と `overflow-y: auto` を与える。
    *   重要な操作ボタンが見切れていないか、最小ウィンドウサイズで必ず確認する。

### 8.2.3 UIイベントシーケンスにおける状態管理の不備
*   **問題**: マウスのクリック操作（`mousedown` -> `mouseup`）とドラッグ操作（`mousedown` -> `mousemove` -> `mouseup`）を区別する際、クリック操作として完了した場合に、`mousedown` で開始された一時的な状態（例: 頂点ドラッグ状態を示す `_draggingVertexInfo`）がリセットされずに残ってしまい、後続の操作で予期せぬ不具合（ゴースト頂点の表示、エラー）を引き起こした。
*   **原因**: `mouseup` イベントハンドラ内で、`_isDragging` フラグが `false`（クリック判定）の場合の処理パスにおいて、`mousedown` で設定された可能性のある `_draggingVertexInfo` をリセットするための処理（`EditingViewModel.endVertexDrag()` の呼び出し）が欠落していた。
*   **解決策**: `mouseup` ハンドラのクリック処理パスの先頭で、もし `_draggingVertexInfo` が設定されている（＝`mousedown` でドラッグが開始されていた）ならば、移動距離に関わらず `EditingViewModel.endVertexDrag()` を呼び出すように修正。`endVertexDrag` 内部で移動距離が閾値以下であれば実際の移動はスキップされ、状態のリセットのみが行われる。
*   **教訓**:
    *   マウス操作のように複数のイベント（`mousedown`, `mousemove`, `mouseup`）で構成されるインタラクションを実装する際は、イベントシーケンスの**すべての可能なパス**（クリック、ドラッグ）を考慮し、各パスで状態が正しく開始・更新・終了（リセット）されることを保証する。
    *   特に、「開始」イベントで設定した状態は、「終了」イベントで**必ず**解除または適切に処理されるように設計する。途中のイベント（`mousemove`）が発生しなかった場合（クリック）の考慮漏れに注意する。
    *   状態遷移の判断には、単純なフラグ（`_isDragging`）だけでなく、関連する状態（`_draggingVertexInfo` が null かどうか）も組み合わせて、より正確なコンテキストに基づいて処理を分岐させる。

#### 8.8.4 一時的なインタラクション状態と描画ロジックの連携不備
*   **問題**: 線上点追加操作で、新しい頂点をエッジに追加してドラッグしている最中、頂点マーカーは移動するが、所属する地物の境界線プレビューが更新されなかった。
*   **原因**: プレビュー描画ロジック (`MapViewRendererHelper.renderDragPreview`) が、汎用的な頂点ドラッグのケースしか想定していなかった。線上点追加という特殊なコンテキスト（新しい頂点がまだ地物のデータモデルに正式に組み込まれていない状態）を認識できず、影響を受ける地物や、更新後の仮の形状を計算できなかった。状態を管理する `EditingViewModel` は、この一時的なコンテキスト情報（`_pendingVertexAdditionInfo`）を保持していたが、それを描画ヘルパーに伝達する仕組みがなかった。
*   **教訓**:
    *   複数のステップからなるUIインタラクション（例: 選択 -> ドラッグ -> 確定）では、各ステップで発生する一時的な状態をViewModelで一元管理する。
    *   プレビュー描画のようなView側のロジックは、ViewModelからこれらの一次的な状態を取得し、それに基づいて描画内容を動的に構築する必要がある。
    *   汎用的な描画ロジック（例: `renderDragPreview`）を実装する際は、それが利用される可能性のある全ての特殊なコンテキストを考慮し、それぞれのケースに対応できるように設計する。あるいは、コンテキストごとに専用の描画ヘルパーメソッドを用意する。
    *   ViewModelは、Viewが必要とする一時的な状態を外部に公開するための適切なインターフェース（ゲッターメソッドなど）を提供すべきである。

### 8.3 データ整合性、型、構造

#### 8.3.1 ドメインオブジェクトの代わりにリテラルオブジェクトを使用
*   **問題**: 地物追加・確定後、保存時や描画時に `property.getAttributes is not a function` や `property.isActiveAt is not a function` エラーが発生した。
*   **原因**: `EditingViewModel.confirmAddFeature` に渡すプロパティ情報を、ドメインの `Property` **クラスインスタンス**ではなく、**単純なJavaScriptオブジェクトリテラル**で作成していたため、`Feature` 内の `properties` 配列に不正な型のオブジェクトが格納された。
*   **教訓**:
    *   DDDにおいて、エンティティや値オブジェクトは定義された**クラスのインスタンス**として扱う。プレゼンテーション層からアプリケーション/ドメイン層にデータを渡す際は、適切なクラスインスタンスを生成する。
    *   配列要素についても期待される型（例: `Property` インスタンス）であることを保証する。型チェック（TypeScript等）の活用が望ましい。

#### 8.3.2 コンポーネント間のデータ構造不一致
*   **問題**: 点情報(`Point`)を追加しても表示されず、保存データで `vertexIds` が `[null]` になっていた。
*   **原因**: `EditFeatureUseCase._processGeometry` は `vertexIds` (配列) を生成したが、`Point.create` は `vertexId` (単数形) を期待していたため、データ構造の不一致により不正な `Point` インスタンスが生成された。
*   **教訓**:
    *   コンポーネント間でデータを渡す際は、プロパティ名、型、構造が受け取り側の期待と一致しているか確認する。特に類似名に注意 (`vertexId` vs `vertexIds`)。
    *   データフローの各ステップでのデータ構造の変化と後続ステップの期待値を明確にする。
    *   ファクトリメソッド等は期待する入力構造を明確化し、呼び出し側はそれに従う。

#### 8.3.3 ドメインオブジェクト内部状態の不整合
*   **問題**: 地物追加後、`Cannot read properties of undefined (reading 'year')` 等のエラーが発生。
*   **原因**: `Feature` オブジェクト内の `properties` 配列に格納された `Property` インスタンスの内部状態 (`_timePoint`) が不正 (`undefined`) になっていた。UI層からドメイン層へのデータ受け渡しや、ドメインオブジェクトのコンストラクタ/メソッドでの状態設定ロジックの不備が考えられる。
*   **教訓**:
    *   ドメインオブジェクト生成時、コンストラクタ引数が正しく渡され、内部状態が期待通りに初期化されているか確認する。
    *   データが複数層を経由する場合、各段階でオブジェクトの状態/型の一貫性を追跡する（デバッガやログ活用）。
    *   値オブジェクト (`TimePoint`, `Property`) が正しく生成されエンティティ (`Feature`) に設定されていることを検証する。

#### 8.3.4 データタイムスタンプの誤り (例: 地物追加時刻)
*   **問題**: 地物追加時に、タイムライン上の時間ではなく、システムの現在時刻がタイムスタンプとして記録された。
*   **原因**: プロパティの `TimePoint` 生成時に、`NavigateTimeUseCase.getCurrentTime()` で取得すべきコンテキスト上の時刻ではなく、`new Date()` などでシステムの現在時刻を参照していた。
*   **教訓**: 時間軸を持つアプリでは、データのタイムスタンプ生成時に、必ず**コンテキスト上の時刻**（タイムラインの現在時刻など）を参照する。時刻源を一元管理するサービス (`NavigateTimeUseCase`等) から取得するよう徹底する。

#### 8.3.5 継承関係におけるメソッドオーバーライド漏れ**
*   **問題**: 基底クラス (`Feature`) のメソッド (`withProperties`) がサブクラス (`Polygon`) の固有状態 (`holesVertexIds`) を考慮せずに新しいインスタンスを生成したため、プロパティ更新時にサブクラスの状態が失われた。
*   **原因**: サブクラスが追加の内部状態を持つ場合、状態を更新する基底クラスのメソッド（特に新しいインスタンスを返すもの）は、サブクラスで適切にオーバーライドして追加状態を引き継ぐ必要があるが、それが漏れていた。
*   **教訓**: クラス継承を使用し、サブクラスが追加の状態を持つ場合、状態を変更したり新しいインスタンスを生成したりする基底クラスのメソッドは、サブクラスでオーバーライドする必要がないか慎重に検討する。特にイミュータブルパターンで `withXxx` のようなメソッドを実装する場合に注意が必要。

#### 8.3.6 アンドゥ/リドゥにおけるデータ復元の不整合
*   **問題**: 地物や頂点の削除操作をアンドゥ/リドゥした際に、対象の要素が正しく復元されない、またはエラーが発生する。特に、削除された地物が必要としていた共有頂点などが復元されず、データ不整合が発生する。
*   **原因**:
    1.  **履歴データの不足**: アンドゥ履歴に、操作対象の要素だけでなく、その要素が依存していた他の要素（例: 削除された地物が参照していた全ての頂点データ）の情報が十分に保存されていない。
    2.  **インスタンスの不適切な再利用**: 履歴にドメインオブジェクトのインスタンスを直接保存し、アンドゥ/リドゥ時にそれを再利用している。これにより、他の操作による意図しない状態変更の影響を受けたり、必要なメソッドが失われたプレーンオブジェクトとして扱われたりする可能性がある。
    3.  **デシリアライズ漏れ**: 履歴からプレーンオブジェクトを復元する際に、ネストされた値オブジェクト（`Property` 内の `TimePoint` など）を再帰的に正しいドメインインスタンスに変換する処理が抜けている。
*   **解決策**:
    1.  **履歴の保存形式**:
        *   履歴には、復元に必要な**プレーンオブジェクト**を保存する（`JSON.stringify`/`parse` や、必要な情報のみを抽出したカスタムオブジェクト）。
        *   **削除操作のアンドゥ履歴**: 削除された要素（地物、頂点）だけでなく、削除された地物が**操作前に参照していた全ての頂点**のデータ（プレーンオブジェクト）も `verticesToRestoreData` のようなキーで履歴に含める。影響を受けた地物の状態 (`affectedFeaturesBefore`) もプレーンオブジェクトで保存する。
    2.  **ドメインオブジェクトの再生成**: アンドゥ/リドゥ時に履歴から状態を復元する際は、保存されたプレーンオブジェクトを元に、必ず各ドメインクラス (`Point`, `Line`, `Polygon`, `Property`, `TimePoint` など) のコンストラクタやファクトリメソッドを呼び出して**新しいインスタンスを生成**する。プレーンオブジェクトを直接データ配列に戻さない。
    3.  **デシリアライズヘルパー**: プレーンオブジェクトからドメインオブジェクト（特に値オブジェクト）を安全かつ再帰的に再生成するためのヘルパーメソッド (`_deserializeFromHistory` など）を実装し、利用する。
*   **教訓**:
    *   アンドゥ/リドゥ機能を実装する際は、「操作を元に戻す」ために**何の情報が必要か**を正確に特定し、それを過不足なく履歴に保存する。依存関係にあるデータ（例: 地物と頂点）の復元漏れに注意する。
    *   状態の不整合を防ぐため、履歴にはプレーンオブジェクトを保存し、復元時には必ず新しいドメインインスタンスを生成するアプローチを原則とする。
    *   データ構造がネストしている場合は、デシリアライズ処理も再帰的に行う必要がある。
    *   履歴に保存するデータは、その形式が一貫している必要があります。例えば、あるデータはシリアライズ済みで、別のデータはプレーンオブジェクトである、といった不整合はデシリアライズ失敗の原因となります。履歴ペイロードを生成する際は、全てのデータが期待される一貫したフォーマット（例: `_constructorName` を持つシリアライズ済み形式）になっていることを保証します。

#### 8.3.7 データ構造と描画ロジックの不整合
*   **問題**: データモデル上は存在する情報（例: MultiPolygon 本体の外周）が、描画ロジックの条件分岐や実装漏れにより、特定の条件下（例: `isMultiPolygon` が `true`）で描画されない。
*   **原因**: データ構造の全ての状態（例: `isMultiPolygon` が `true` かつ `vertexIds` も持つケース）を描画ロジックが網羅できていない。条件分岐が排他的すぎたり、特定の組み合わせの実装が漏れている。
*   **教訓**: データ構造を定義・変更する際は、そのデータを取り扱う全ての箇所（特に描画、シリアライズ、操作ロジック）で、新しい状態や全ての有効な組み合わせが適切に処理されるかを確認する。条件分岐は排他的ではなく、組み合わせを考慮するように記述する（例: `if (isMulti)` と `if (hasVertices)` を独立してチェックする）。
*   **教訓**: 仕様から削除されたメタデータ（例: `Property.attributes.category`）を UI・保存・レンダリングのどこか一箇所でも残すと、レイヤー設定だけで地物の性質を判定できなくなり、古い情報源が復活してしまう。`src/presentation/views/MapView.js` や `src/presentation/views/sidebar/PropertiesTabView.js` がカテゴリ入力を維持し、`src/infrastructure/rendering/RenderStyleProvider.js` がそれに依存した結果、仕様上は不要なカテゴリ概念が実態として残留した。不要になった属性は UI→ビューモデル→永続化→レンダリングの順に必ず除去し、新しい情報源（今回なら `Layer` のスタイル設定）を導入してから利用箇所を切り替えること。

#### 8.3.8 履歴データ生成時のソース特定ロジックの曖昧さとJavaScriptのTruthiness/Falsinessの罠
*   **問題**: 地物（特にポリゴン）追加操作のアンドゥ履歴に、追加された頂点の情報 (`addedVerticesData`) が正しく記録されず（空配列になるなど）、Redo時に頂点が復元できなかった。
*   **原因**: アンドゥ履歴を作成する際、追加された地物オブジェクト (`feature`) から頂点IDリストを取得するロジックに問題があった。具体的には、`feature.vertexIds || feature.rings?.flatMap(r => r.vertexIds || [])` のようなJavaScriptのOR演算子 (`||`) を使った短絡評価に依存していた。ポリゴンの場合、`feature.vertexIds` は（基底クラスのプロパティとして存在はするが）通常は空配列 `[]` である。JavaScriptでは空配列 `[]` はオブジェクトであり、オブジェクトは **truthy** な値として評価される。そのため、`feature.vertexIds || ...` という式は、`feature.vertexIds` が `[]` の場合でもその `[]` を返し、期待していたポリゴンのリング情報 (`feature.rings?.flatMap(...)`) が評価・使用されていなかった。
*   **解決策**: 地物オブジェクトから履歴記録用の頂点IDリストを抽出するロジックを、地物の種類（例: `this._editingViewModel.getTool()` の結果）に基づいて明示的に分岐するように修正した。ポリゴンの場合は `feature.rings` から、ポイントやラインの場合は `feature.vertexIds` から頂点IDを取得するようにした。
*   **教訓**:
    *   オブジェクトから特定のデータ（特に、オブジェクトの型や状態によって格納場所やプロパティ名が異なる可能性があるもの）を抽出する際は、OR演算子 (`||`) の短絡評価のような暗黙的なロジックに頼らず、型や状態を明示的に判定して適切なパスからデータを取得することが重要である。
    *   JavaScriptにおける値のtruthiness/falsiness（真偽値としての評価）を正確に理解し、特に `[]` (空配列) や `{}` (空オブジェクト) がtruthyとして扱われる点に注意する。`null`, `undefined`, `0`, `NaN`, `""` (空文字列), `false` のみがfalsyである。
    *   ポリモーフィズムを利用している場合（例: `Feature` のサブクラス `Point`, `Line`, `Polygon` がそれぞれ異なる方法で頂点情報を保持している場合）、各サブクラスがデータをどのように保持しているかを正確に把握し、それに応じたアクセス方法を条件分岐で使い分ける。
    *   コードの可読性と保守性の観点からも、曖昧さを生む可能性のあるトリッキーな短絡評価よりも、意図が明確な条件分岐を用いる方が望ましい場合が多い。

#### 8.3.9 イミュータブルエンティティのAPI不備によるユースケースの停止
*   **問題**: ManageLayersUseCase.deleteLayer でレイヤーを削除すると TypeError: world.layers[i].withOrder is not a function が発生し、処理が停止した。
*   **原因**: レイヤー情報をイミュータブルに扱う前提でユースケース側のロジックが Layer.withOrder を呼び出すよう改修されたが、Layer クラスに対応するメソッドを追加し忘れていたため、期待するAPIが存在しなかった。
*   **教訓**:
    *   イミュータブルなエンティティに withXxx 系メソッドを導入する際は、全ての属性について必要なメソッドが網羅されているかを洗い出し、ユースケースが呼び出すAPIとの整合を確認する。
    *   ドメイン層のAPIを拡張するリファクタリングでは、呼び出し側と提供側の変更が揃っているかをテストや静的解析で検証し、実行時例外を未然に防ぐ。

#### 8.3.10 ロールバックでイミュータブル頂点を再配置した
*   **問題**: プロパティ編集の検証エラーで `_restoreWorldVertices` が `world.vertices` を復元した後、頂点移動 (`VertexEditUseCase.moveVertices`) が `TypeError: Cannot set property x of [object Object] which has only a getter` で失敗した。
*   **原因**: ロールバック処理が `new Vertex(...)` を生成しており、`Vertex` は `Object.freeze` によって座標をゲッターのみのイミュータブル状態にしているため、世界データが書き換え不可のインスタンスに置き換わってしまった（`src/application/usecases/feature/UpdateFeatureUseCase.js:223-224`）。後続処理は `world.vertices` がプレーンオブジェクトである前提で座標を直接更新するため、例外が発生する。
*   **教訓**: 世界データや履歴スナップショットの復元では、後続ユースケースが期待する可変なプレーンオブジェクト構造に戻す。ドメインエンティティ（`Object.freeze` 済み）を共有キャッシュへ直接入れず、必要に応じてプレーンデータへ変換する。

#### 8.3.11 初期世界のレイヤーがプレーンオブジェクトのままだった
*   **問題**: 保存ファイルが存在しない状態でアプリを起動し、既定レイヤーの名称変更や削除を試みると `TypeError: currentLayer.withName is not a function` が発生し、ManageLayersUseCase が処理を完了できなかった。
*   **原因**: `JSONWorldRepository._createEmptyWorld` がレイヤーをプレーンオブジェクトで生成しており、`ManageLayersUseCase.updateLayer`（`src/application/usecases/ManageLayersUseCase.js:58-93`）が期待する `Layer` エンティティを受け取れなかった。該当コードは `src/infrastructure/persistence/JSONWorldRepository.js:82-104` に示す通り、`new Layer(...)` を用いずにリテラルを返していた。
    ```js
    layers: [
      {
        id: "layer-base",
        name: "基本レイヤー",
        order: 0,
        visible: true,
        opacity: 1.0,
        description: "最初のレイヤー"
      }
    ]
    ```
*   **教訓**:
    *   ドメインエンティティ化したデータ構造に依存するユースケースを導入した際は、初期データ・テストデータ・スタブがすべて同じエンティティ型になるよう即座に整合させる。特に `_createEmptyWorld` のようなデフォルト生成コードは見落としやすい。
    *   共有キャッシュやリポジトリが返すオブジェクトは、アプリ全体で期待される型に統一する。プレーンオブジェクトとエンティティが混在すると、イミュータブルAPI呼び出しで即座に破綻する。

#### 8.3.12 飛び地を持つポリゴンが排他チェックで恒常的に拒否された
*   **問題**: 同一ポリゴン内の穴に飛び地（領土リング）を追加すると、`LayerService.checkExclusivity` が「同じレイヤー内で地物が重複している」と判定し、`UpdateFeatureUseCase` からのリング追加が失敗した。
*   **原因**: `LayerService.checkExclusivity`（`src/domain/services/LayerService.js:217-275`）がリング階層を無視して全ての領土リング同士の内包を禁止していた。正規の構造では「領土 → 穴 → 飛び地」のように穴を一段挟んだ入れ子が許容されるが、アルゴリズムが `ring.parentId` と `ringType` を参照していなかったため、飛び地も「不正な内包」と見なされていた。
*   **教訓**:
    *   リングベース構造を扱うロジックでは、`parentId` と `ringType` による階層（領土・穴の交互構造）を常に考慮する。幾何的な結果だけで判定すると、合法な入れ子（穴の中の飛び地など）まで排除してしまう。
    *   判定の対象となる組み合わせが親子関係を持つかどうかを事前に分類し、同一階層（兄弟）だけを厳密に比較する。祖先・子孫関係にあるリング同士は、適切な穴を介しているかを確認した上で例外扱いにする。
    *   リング階層を前提としたロジックを変更した場合は、穴・飛び地・多段ネストなど代表的な組み合わせをドメインテストで網羅し、合法パターンが継続して通ることを自動化する。

#### 8.3.13 JSON復元後の `world.vertices` を `Vertex` インスタンスのまま保持した
*   **問題**: アプリを再起動して JSON からワールドを読み込んだ直後に頂点を移動すると、`VertexEditUseCase.moveVertex`／`moveVertices` が `TypeError: Cannot set property x of [object Object] which has only a getter` を投げ、線情報の編集が継続できなかった。
*   **原因**: `JSONSerializer._deserializeVertex`（`src/infrastructure/persistence/JSONSerializer.js:162-170`）が `new Vertex(...)` を返していた。`Vertex` はコンストラクタで `Object.freeze` 済みのイミュータブルオブジェクトであるため、永続化層から復元しただけで `world.vertices` が「ゲッターのみ」の状態になり、ユースケースが前提とするプレーンな `{id, x, y}` を書き換えられなくなっていた。
*   **教訓**:
    *   永続化境界では、キャッシュに保持するデータ構造がミューテーションを受け入れられるかどうかを必ず明示し、編集ユースケースが期待するプレーンオブジェクトに合わせる。イミュータブルなドメインエンティティを共有キャッシュに戻すと、参照を共有する全レイヤーで一斉に破綻する。
    *   シリアライザのリファクタリング時は「復元直後に座標を書き換える」ような回帰テストを追加し、`expect(() => vertex.x = ...)` で可変性を保証する。永続化→編集のホットパスは少量のテストで良いので確実に守る。

#### 8.3.14 参照契約リファクタ時の呼び出し側取りこぼし
*   **問題**: `Property` から `FeatureAnchor` への契約移行後、`tests/presentation/editingViewModelSplit.test.js` の呼び出しが `splitMethods.confirmSplit.call(context, 1, property, editTime)` のまま残り、`ReferenceError: property is not defined` でテストが失敗した。
*   **原因**: `EditingViewModelSplit.confirmSplit` の引数契約を `newAnchor` に変更した一方で、呼び出し側の識別子更新が1箇所漏れていた。
*   **教訓**:
    *   入力契約を型ごと切り替えるリファクタでは、定義側の変更と同時に呼び出し側を `rg` で機械的に列挙し、旧引数名・旧型名の残存をゼロにする。
    *   変数名置換を含む変更では、テストの呼び出し式（`call(...)` など）を最終確認し、旧識別子が1件も残っていないことを完了条件にする。

### 8.4 依存関係と設定

#### 8.4.1 依存性の注入漏れとコンストラクタ定義の不備
*   **問題**: `SidebarView` 内で `this._editingViewModel` のメソッドを呼び出そうとして `TypeError` が発生。
*   **原因**: `SidebarView` のコンストラクタ引数に `editingViewModel` がなく、インスタンスを保持していなかった。また、`DependencyInjection` でインスタンス化する際に `EditingViewModel` が注入されていなかった。
*   **教訓**:
    *   クラスが依存性を持つ場合、コンストラクタで受け取り内部プロパティとして保持する。
    *   DIコンテナ等でインスタンス化する際は、コンストラクタのシグネチャと渡す依存性が正確に一致しているか確認する。
    *   クラスの責務と依存関係を明確にし、正しく注入されるように設計・実装する。

#### 8.4.2 依存クラスのインポート漏れ
*   **問題**: レンダラーやビューで `instanceof` 演算子や `new` 演算子を使用する際に `ReferenceError: X is not defined` が発生。
*   **原因**: 利用するクラス（例: `Polygon`, `Line`, `Point`, `TimePoint`）が、そのファイル内で `import` されていなかった。
*   **教訓**: 外部モジュールや別ファイルで定義されたクラス・関数・定数などを利用する際には、必ずファイルの先頭で適切に `import` 文を記述する。特に `instanceof` でクラス自体を参照する場合や、複数の関連クラスを扱う場合に忘れやすい。依存関係を明確にし、漏れがないか確認する。

*   **事象**: `Vertex.getCoordinate()` を呼び出すと `Coordinate is not defined` が発生した。
*   **原因**: `src/domain/entities/Vertex.js` で `Coordinate` をインポートしておらず、`new Coordinate(...)` が未定義参照になっていた。
*   **教訓**: エンティティから別ファイルの値オブジェクトを生成する場合でも依存モジュールを必ず明示的にインポートする。ファイル先頭の `import` 漏れがないかチェックする。

#### 8.4.3 テストが非公開APIに依存した
*   **問題**: `MapViewRendererHelper._unwrapLongitudeSequence` のような非公開ヘルパーをテストから直接参照し、内部リファクタだけでテストが破綻した。
*   **原因**: 公開API経由で振る舞いを検証する設計がなく、非公開メソッドの仕様を固定化してしまった。
*   **教訓**:
    *   テストは公開APIや公開契約に対して振る舞いを検証する。非公開実装の詳細に依存しない。
    *   内部ロジックの検証が必要な場合は、公開ユーティリティとして契約を設けるか、公開メソッド経由で観察可能な出力を用意する。

### 8.5 環境依存とフレームワーク制約

#### 8.5.1 Electron環境での同期ダイアログ使用エラー
*   **問題**: 地物追加確定時に `prompt()` を使用したところ、Electron環境で `Error: prompt() is not supported.` が発生。
*   **原因**: ElectronのRendererプロセスでは `window.prompt()` 等の同期ダイアログ関数はデフォルトで無効化されている。プロパティ入力用の独自UIが未実装だったため暫定使用したコードが抵触した。
*   **教訓**:
    *   Electron開発ではRendererプロセスの制約（Webブラウザに近い）を意識する。標準の同期ダイアログは使用不可と考える。
    *   ユーザー入力/確認には独自のUI要素を実装するか、Electronの `dialog` モジュール等の代替手段を検討する。
    *   環境依存性の高い暫定コードはリスクを認識し、早期に適切な実装に置き換える。

### 8.6 パフォーマンスと計算ロジック

#### 8.6.1 ズームレベルに応じた要素サイズ計算の不統一
*   **問題**: 点半径やラベルフォントサイズが `Math.sqrt(zoom)` でスケールされ、ズーム操作で要素サイズが不適切に変化し、視認性の問題を引き起こした。
*   **原因**: 画面上でのサイズを一定に保つ計算 (`1 / zoom`) と、ズームに応じて変える計算 (`1 / Math.sqrt(zoom)`) が意図せず混在し、設計方針が不明確だった。
*   **解決策**: 全要素で「画面上での見た目を一定に保つ」方針とし、SVG属性値を `基準値 / zoom` でスケーリングするよう統一。フォントサイズには最小/最大値の制限を設けた。
*   **教訓**: ズーム機能を持つSVG描画では、要素の画面上でのサイズをどう扱うか（固定/可変）を明確に定義し、スケーリング計算を一貫させる。`viewBox` 使用時、SVG属性値は `基準値 / zoom` で画面上の見た目を一定に保てる。

####  8.6.2 ビューポート設定の動的計算漏れ (例: 初期/最小ズーム)
*   **問題**: アプリケーション起動時のズームレベルや、ズームアウト可能な最小レベルが固定値であり、画面サイズによっては表示範囲が広すぎたり、地図の左右に余白が見えたりした。
*   **原因**: 初期ズームレベルや最小ズームレベルを、コンテナ（画面）のサイズや地図データの幅に基づいて動的に計算せず、ハードコードされた値や設定ファイルからの固定値のみを使用していた。地図全体を表示するために必要なズームレベルを下回ることがあった。
*   **教訓**: ビューポートのズームレベル（特に初期値と最小値）は、画面サイズや地図のワールド座標幅に依存する場合があるため、固定値ではなく動的に計算・調整する必要がある。ユーザー設定値（意図）と計算上の制約（表示要件）の両方を考慮し、`Math.max` や `Math.min` などを用いて最終的な有効値を決定する。

**8.6.3 ズームレベル依存の閾値管理の不整合**
*   **問題**: クリックとドラッグの区別、スナップ判定、近接判定など、画面上のピクセル距離やワールド座標上の距離に基づく閾値判定が、ズームレベルの変更に対して一貫性のない動作をする。あるズームレベルでは適切でも、別のレベルでは意図通りに機能しない（例: ドラッグが開始しにくい、意図せずスナップする）。
*   **原因**:
    1.  閾値を固定ピクセル値で定義しており、ズームによるワールド座標とのスケール変化を考慮していない。
    2.  閾値をワールド座標で定義しているが、その計算や適用がズームレベルに応じて適切に更新されていない。
    3.  異なるコンポーネント（例: ViewとViewModel）で同じ目的の閾値が別々に定義・管理され、値や計算方法が同期していない。
*   **教訓**:
    *   距離に基づく閾値は、それが**画面上の操作感**（例: ドラッグ開始のしきい値）に関わるのか、**ワールド座標上の意味**（例: スナップ範囲）に関わるのかを明確にする。
    *   画面上の操作感に関する閾値はピクセル単位で定義し、必要に応じてズームレベルから逆算してワールド座標での閾値に変換する。
    *   ワールド座標上の意味を持つ閾値はワールド単位で定義し、画面上での判定が必要な場合はズームレベルを考慮してピクセル値に変換する。
    *   関連する閾値は一元的に管理するか、コンポーネント間で適切に連携・同期させるメカニズム（例: ViewModelからViewへ閾値を渡す）を設ける。閾値の計算ロジックを共通化する。

### 8.7 選択状態管理の不整合

*   **問題**: `MapView` で頂点を選択した直後に `selectFeature` を呼び出した結果、`MapViewModel` 内で意図せず頂点選択がクリアされ、Deleteキー押下時に頂点ではなく地物全体が削除される不具合が発生した。
*   **原因**: 選択状態（アクティブ地物と頂点選択コンテキスト）の管理責務が `MapViewModel` に集約されておらず、`MapView` 側のイベントハンドラで不適切な状態遷移を引き起こしていた。また、`MapViewModel` の選択メソッド (`selectFeature`, `selectVertex`) が相互に状態をリセットする副作用を持っていた。
*   **解決策**:
    1.  `MapViewModel` に選択状態（`activeFeatureId`, `selectedVertexIds`, `vertexSelectionContextId`）を明確に定義し、状態間の遷移ロジック（例: 頂点選択時はアクティブ地物を解除し、頂点選択コンテキストを更新）を `MapViewModel` 内に集約。
    2.  `MapView` は ViewModel の選択メソッドを呼び出すのみとし、状態の直接操作や複雑な遷移ロジックを持たないように修正。
    3.  描画 (`_renderSelection`) は ViewModel から最新の状態を取得して行うように統一。
*   **教訓**:
    *   UIの状態管理ロジックは ViewModel に集約し、View は ViewModel の状態を購読して描画に徹する（単方向データフローの原則）。
    *   複数の関連する状態（例: 地物選択、頂点選択、ハイライト）を持つ場合、状態遷移のルールを明確に定義し、一箇所（ViewModel）で管理する。
    *   メソッドの副作用（例: `selectFeature` が頂点選択をクリアする）を明確にし、意図しない状態変更を防ぐ。

**8.8 UIインタラクションと状態管理の連携不備**

**8.8.1 複数選択状態と単一操作の不整合 (例: 複数頂点選択後の単一ドラッグ)**
*   **問題**: UI上では複数要素（例: 頂点）が選択されているように見えても、後続の操作（例: ドラッグ）が単一要素しか対象とせず、ユーザーの意図（複数要素の同時操作）と異なる結果になる。
*   **原因**: 選択状態の管理（ViewModel）と、その状態を利用する操作ロジック（Viewのイベントハンドラ、ViewModelの操作メソッド、UseCase）の間で、複数選択を前提とした処理が実装されていない。単一選択時のロジックが複数選択時にも流用され、予期せぬ動作（選択解除や単一要素のみの操作）を引き起こす。
*   **教訓**:
    *   複数選択をサポートする機能では、選択状態の取得だけでなく、その後の操作（移動、削除、プロパティ変更など）も複数選択に対応させる必要がある。
    *   イベントハンドラでは、操作開始時に現在の選択状態（単一か複数か）を確認し、適切な処理パス（単一操作用メソッド or 複数操作用メソッド）に分岐させる。
    *   ViewModelやUseCaseには、複数要素を一度に扱うためのAPI（例: `moveVertices`, `deleteVertices`）を必要に応じて用意する。
    *   UIの見た目（複数選択されているように見える）と内部的な処理能力（単一しか扱えない）の乖離がないように設計・実装する。

### 8.9 UseCase と ViewModel/View の責務分担の曖昧さ

#### 8.9.1 検証ロジックの配置場所の不統一 (例: 穴追加検証)
*   **問題**: 本来データ整合性の保証のために UseCase 層で行うべき検証（例: 穴がポリゴン内部にあるか、自己交差しないか）が、プレゼンテーション層 (View/ViewModel) で部分的に行われたり、あるいは完全に欠落していた。
*   **原因**:
    1.  **UXのための事前チェックとデータ整合性のための最終検証の混同**: Viewでの入力制限（例: ポリゴン外部をクリックできないようにする）はUX向上に寄与するが、それだけではデータの整合性を保証できない。最終的な検証はデータ永続化直前（UseCase）で行う必要があるが、その実装が漏れていた。
    2.  **責務分担の曖昧さ**: 形状の妥当性チェックをどの層が責任を持つべきか不明確だった。
*   **教訓**:
    *   **入力時の事前チェック (View/ViewModel)**: ユーザー体験を向上させるため、明らかに無効な入力（例: ポリゴン外部への穴追加クリック）はUIレベルで制限する。ただし、これは補助的なものと位置づける。
    *   **確定時の最終検証 (UseCase)**: データの整合性を保証する責任は UseCase（またはドメインサービス）が持つ。地物の追加・更新処理の内部で、関連する全てのビジネスルールと整合性制約（内外判定、交差判定、排他性など）を**必ず**検証する。プレゼンテーション層でのチェックに依存しない。
    *   検証ロジックは `GeometryService` や `LayerService` などの適切なドメインサービスに実装し、UseCase から呼び出す形にする。

#### 8.9.2 データ生成ロジックの不適切な配置 (例: 頂点ID生成)
*   **問題**: 新しい頂点のID生成や `world.vertices` への追加が、本来データ操作の起点となるべき UseCase ではなく、ViewModel (`addHoleToPolygon`内など) で行われていた。
*   **原因**: 穴追加や飛び地追加の処理フローの中で、新しい頂点を生成する必要があったが、その責務を ViewModel が一時的に担ってしまっていた。
*   **解決策**: `EditFeatureUseCase._processGeometry` を拡張し、新しい穴や新しい飛び地のための頂点データ（座標のみ）を受け取り、そこでID生成と `world.vertices` への追加を行うように修正。ViewModel は座標データを渡し、UseCase が生成した頂点IDを受け取って後続の処理（ポリゴン更新など）を行う。
*   **教訓**:
    *   データの生成（特にID割り当て）や永続化に関わる基本的な操作は、UseCase 層またはそれが呼び出すリポジトリ層の責務とする。
    *   ViewModel や View は、ユーザー入力の収集や UseCase への指示伝達に注力し、データの生成・永続化ロジックを直接持たないようにする。
    *   UseCase は、受け取った指示に基づいて必要なデータの生成・検証・永続化を行い、結果（生成されたIDや更新後のオブジェクトなど）を返す。

#### 8.9.3 状態更新フローの不備 (例: 飛び地追加後のデータ不整合)
*   **問題**: 飛び地を追加する際、ViewModel が UseCase を呼び出してポリゴンを更新した後、アンドゥ履歴に追加するデータやイベントバスで通知するデータが、UseCase による実際の更新内容（新しく生成された頂点ID、更新された `subPolygons` 配列）と同期しておらず、不整合な状態を引き起こす可能性があった。
*   **原因**: ViewModel が UseCase 呼び出し**前**の古い状態に基づいて履歴を作成したり、イベントを発行したりしていた。
*   **解決策**: UseCase (`updateFeature`) が、実行した更新の結果（更新後のポリゴンインスタンス全体、または少なくとも追加された部分の詳細情報）を正確に返すようにする。ViewModel は、UseCase から返された**更新後のデータ**に基づいてアンドゥ履歴を作成し、イベントを発行する。
*   **教訓**:
    *   状態を変更する操作（特に UseCase 呼び出し）を行った後は、必ず**操作後の最新の状態**に基づいて後続処理（履歴記録、イベント発行、UI更新）を行う。
    *   UseCase は、実行した操作の結果として状態がどう変化したかを明確に呼び出し元（ViewModel）に伝える責務を持つ。これには、更新後のオブジェクトインスタンス全体や、生成されたIDなど、後続処理に必要な情報が含まれる。
    *   非同期処理 (`async/await`) を使う場合は特に、処理の前後での状態の一貫性に注意する。`await` の前後でデータが変更されている可能性があるため、常に最新のデータを参照するように心がける。

### 8.9 UseCase と ViewModel/View の責務分担の曖昧さ

#### 8.9.4 UIイベントハンドラからViewModelへの情報伝達不足
*   **問題**: 穴の中に飛び地を追加しようとしても、飛び地の親が穴にならず、常にポリゴン本体の直属として扱われた。
*   **原因**: `MapViewEventHandler` が、ユーザーがクリックした位置（穴の内側）から親となるべきリングのIDを特定していたにも関わらず、その情報を `EditingViewModel` に渡さず、常に `null` を渡していた。さらに `EditingViewModel` の確定メソッド側も `null` をハードコードしており、二重に情報が失われていた。
*   **教訓**:
    *   UIイベントハンドラ（View層）は、ユーザーの操作コンテキストから得られる**全ての関連情報**を、状態遷移の指示と共にViewModelに伝達する責務を持つ。情報の取捨選択をView層で早計に行ってはならない。
    *   ViewModelやUseCaseのメソッドは、ハードコードされた値に依存するのではなく、UI層から渡されたコンテキスト情報や自身が管理する状態に基づいて、動的に処理内容を決定すべきである。
    *   たとえ描画が正しく見えても、データモデルの整合性を常に疑うべきである。描画ロジックの実装詳細（例: `fill-rule="evenodd"`）が、データ不整合を隠蔽する可能性を考慮する。

##### 8.9.5 UseCaseの再利用性と冪等性
*   **問題**: Redo操作のように、一度実行した操作を再現しようとした際に、意図しない副作用（例: 新しいIDの生成）が発生し、後続の操作に不整合を引き起こした。
*   **原因**: UseCaseのメソッドが、初回実行時と再現（Redo）時で区別なく、常に新しいIDを生成するなど、冪等でない操作を行っていた。
*   **教訓**:
    *   アンドゥ・リドゥ機能を持つアプリケーションでは、UseCaseの操作は冪等であるか、または冪等性を制御できる（例: IDを外部から注入できる）ように設計することが重要です。
    *   特に、ID生成、タイムスタンプ記録などの副作用を伴う操作は、Redo時に元の値を再利用できるよう、オプションの引数で外部から注入できるインターフェースを提供することを検討します。
    *   これにより、UseCaseの再利用性を高めつつ、状態の復元操作（Redo）の正確性を保証できます。

##### 8.9.6 TimeServiceの繰り下がりロジック欠落
*   **問題**: タイムラインで過去方向に移動すると `TimePoint` の `day` が負数になり、`TimeChanged` を購読するビューと永続化が破綻した。
*   **原因**: `src/application/usecases/NavigateTimeUseCase.js:41-52` で後退操作も `advanceTime` を経由しているにも関わらず、`src/domain/services/TimeService.js:140-184` の `advanceDays` が `newDay <= 0` の借り入れ処理を実装していなかった。
*   **教訓**:
    *   時刻や座標のように循環する値を扱うロジックでは、増分方向だけでなく減分方向の繰り上げ/繰り下げも実装し、`TimePoint` のような値オブジェクトの不変条件を常に維持する。
    *   ユースケースがドメインサービスを呼び出す際には、`retreatTime` のように符号を反転させて再利用する経路でも仕様が破綻しないか境界テストを用意して確認する。

##### 8.9.7 TimeServiceの年次境界における分解能不足
*   **問題**: 年のみ指定された `TimePoint` に対して `NavigateTimeUseCase.advanceTime(365)` を繰り返しても、年が更新されず時間制御が停止した。
*   **原因**: `src/domain/services/TimeService.js:239-256` の「月日未指定」分岐が `daysPerYear=365.25` を単純に `Math.floor(totalDays / daysPerYear)` に渡し、1 年弱の加算が常に 0 年として切り捨てられていた。
*   **教訓**:
    *   平均値や実数を用いて周期を表現する際は、単純な切り捨てで境界値が失われないよう、許容範囲を設けるか残差を繰り越す仕組みを実装する。
    *   特にドメインサービスは UI の更新頻度に依存しないため、連続する呼び出しで計算結果が停滞しないことを境界テスト（`tests/domain/timeService.test.js`）で保証する。

##### 8.9.8 TimeServiceの負方向丸めの偏り
*   **問題**: 年のみ指定された `TimePoint` を `NavigateTimeUseCase.retreatTime(400)` で 400 日分戻すと、実際には 2 年分巻き戻ってしまい、年を余計に借りる挙動が発生した。
*   **原因**: `src/domain/services/TimeService.js:343-368` の年換算で、負方向でも `Math.floor(rawYears + tolerance)` を使用しており、`rawYears=-1.095...` のようなケースが `-2` に切り捨てられていた。負方向では `Math.ceil` 相当の処理に切り替える必要があった。
*   **教訓**:
    *   切り捨て補正用のトレランスを導入する場合は、正負双方の丸め方向を明示的に設計し、対称性をテストで担保する。`advanceDays` には 1 年を超える差分を与えるテストケースを追加する。
    *   同一のユースケース（`retreatTime`）が正負両方向の処理を共有する際には、正方向だけでなく負方向の境界条件（年を跨ぐギリギリの値）も自動テストへ追加しておく。

##### 8.9.9 大圏経路描画で経度の折り返し補正を失念した
*   **問題**: 測定ツールで日付変更線を跨ぐと、大圏コースの表示が地図を一周してしまい、描画が現実離れした。
*   **原因**: `src/presentation/views/map/MapViewRendererHelper.js:423-434` で `GeometryService.calculateGreatCirclePath` の出力をそのまま `drawLine` に渡しており、経度が 179° から -179° に跳ぶ区間でも補間せず折り返してしまっていた。
*   **教訓**:
    *   日付変更線を跨ぐ経路では「表示に最適化した経度の連続性」を別途用意する。`renderDistanceMeasurement` のように UI で線を引く箇所は、取得した座標列に対して逐次的なアンラップ処理を挿入し、連続差分が ±180° を超えたら ±360° を加減して線分を補正する。
    *   ドメインサービスが正規化した座標を返す場合でも、消費側（UI）が求める座標系と一致するかを明示し、描画方向で必要な変換を責務として追記する。

##### 8.9.10 分割線の向きとリング順序の不整合
*   **問題**: 分割線に複数の内部点がある場合、分割結果の面が自己交差として扱われた。
*   **原因**: `src/domain/services/PolygonSplitService.js` の `buildPolygonSplitPlan` が交点のリング順で `path1`/`path2` を構築する一方で、`cutLine` を入力順の向きのまま `cutInterior` に結合し、交点の順序が逆の場合にリングの頂点列が反転した。
*   **教訓**:
    *   交点の順序とリングの順序が別の軸で決まる場合は、結合前に向きを正規化し、リング側の順序に合わせて補間点列を並べ替える。

##### 8.9.11 分割ロジックが外周リングのみを前提にしていた
*   **問題**: 分割ツールが外周リング1つしか扱えず、穴・飛び地を持つ面情報の分割が失敗した。
*   **原因**: `src/domain/services/PolygonSplitService.js` と `src/application/usecases/feature/SplitPolygonUseCase.js` が `rings[0]` 前提で実装され、リング階層の再構成が未実装だった。
*   **教訓**:
    *   トポロジーを変える操作では、複数リングと親子関係を前提にしたデータモデルと、ブーリアン演算などの幾何ロジックをセットで設計する。
    *   分割/結合のように形状全体が変わる処理は、ユースケース・描画・整合性検証が新しいリング構造に追従しているかを同時に確認する。

##### 8.9.12 閉線分割の導線が途中で途切れた
*   **問題**: 分割線の2点目入力時に閉線分割用の目印が消え、閉線分割へ切り替えられなかった。
*   **原因**: `src/presentation/views/map/MapViewRendererHelper.js` が `addingPoints.length === 1` の場合だけ円を描画し、`src/presentation/views/map/MapViewEventHandler.js` も円内クリックの判定を同条件に固定していた。
*   **教訓**:
    *   入力中の切替UIは「入力中」という状態に紐づけて保持し、途中段階で視覚フィードバックと判定条件がずれないようにする。
    *   トリガー条件を狭くする場合は、操作フロー（2点目以降も切替可能か）を仕様に明記して実装を揃える。

##### 8.9.13 分割線の内外判定がUIとドメインで統一されていなかった
*   **問題**: 穴内部から分断線を開始できず、入力が進められなかった。
*   **原因**: `src/presentation/views/map/MapViewEventHandler.js` と `src/domain/services/PolygonSplitService.js` が「面の内外」の定義を外周リング基準に固定していたため、穴内部を一律で面内として扱っていた。
*   **教訓**:
    *   内外判定が「塗りつぶし領域」と「外周領域」のどちらを指すかを明文化し、UI/ドメインで同じ判定ロジックを共有する。

##### 8.9.14 分割ガイド円が入力形状を上書きした
*   **問題**: 分断線入力中にガイド円内をクリックすると、ユーザーが入力した分断線が円形の分割線に置き換わり、意図した閉線分割にならなかった。
*   **原因**: `src/presentation/views/map/MapViewEventHandler.js` がガイド円内クリックを「円形分割線の生成」と解釈し、`setAddingPoints` で入力済みの分断線を上書きしていた。
*   **教訓**:
    *   入力補助UI（ガイド円など）は「状態切替」や「確定トリガー」として扱い、入力済みの形状データを置き換えない。
    *   仕様上の「ガイド」「切替」「生成」を実装に落とし込む際は、ユーザー入力の保持と置換の境界を明示する。

##### 8.9.15 閉線切替が描画に反映されなかった
*   **問題**: ガイド円内クリックで閉線モードに切り替えても、最新点と始点を結ぶパスが描画されず、閉線になったことが視認できなかった。
*   **原因**: `src/presentation/views/MapView.js` が `splitLineMode` の変更を描画更新対象に含めておらず、`EditingViewModel.setSplitLineMode` の通知が描画へ反映されなかった。
*   **教訓**:
    *   描画に影響する状態フラグを追加したら、通知側のイベント名と購読側の再描画トリガーを必ずセットで更新する。

##### 8.9.16 分断線の自己交差が分割結果の重なりを引き起こした
*   **問題**: 分断線が自己交差した状態で確定すると、分割結果の面情報が重なり、排他性検証で失敗した。
*   **原因**: `src/domain/services/PolygonSplitService.js` が分断線の自己交差を検証せず、自己交差した線分から分割面を生成していた。
*   **教訓**:
    *   分割のようなトポロジー変更では、入力線が単純な線であることを事前に検証し、自己交差や重複がある場合は確定前に拒否する。
    *   入力条件違反はドメインサービスで明示的に弾き、後段の排他性検証に責務を押し付けない。

### 8.10 状態更新ロジックにおける依存関係の不完全な処理
*   **主題**: 状態更新ロジックにおける依存関係の不完全な処理
*   **問題**: 階層構造を持つデータ（ポリゴンのリング親子関係など）の一部を削除する際に、その要素に依存する他の要素（子要素）の扱いが定義されておらず、エラーが発生したり、データが不整合な状態になったりした。
*   **原因**: 削除ロジックが、削除対象の子要素の存在をチェックしてエラーを投げるだけで処理を中断していた。また、呼び出し元がそのエラーを握りつぶし、不完全な状態で処理を続行していた。
*   **教訓**:
    *   階層構造や依存関係を持つデータを操作する場合、要素の削除が依存要素にどのような影響を与えるかを明確に定義し、処理ロジックに組み込む必要がある（例: 連鎖削除、親の再割り当て、操作の禁止など）。
    - ドメイン層で発生した整合性に関するエラーは、アプリケーション層で安易に握りつぶさず、操作全体を失敗させるか、ユーザーに解決を促すなど、適切にハンドリングする。
    - 単純なエラーチェック（例: `hasDependentChildren`）だけでなく、依存関係を解決する具体的なロジック（例: 連鎖削除）を実装することが、堅牢なシステム構築には不可欠である。
    - 実装中にこのような状況を発見した場合、実装を中断してでもユーザーに解決策を訊ねるべきである。

### 8.11 テストが実装詳細に依存したことで破綻した
*   **問題**: テストが `EditFeatureUseCase` の private フィールドや内部委譲先を直接差し替え、内部構造の変更で即座に破綻した。
*   **原因**: 依存差し替えや参照のための公開契約が存在せず、テストが実装詳細に依存する構造になっていた。
*   **教訓**:
    *   依存差し替えは公開API（引数・getter）として提供し、private フィールドへ直接アクセスしない。
    *   ViewModel は UseCase の公開契約を通じてリポジトリへアクセスし、内部構造の変更に耐える設計にする。

### 8.12 ドラッグ中の幾何計算を毎回再構築して性能が劣化した
*   **問題**: 頂点ドラッグ中の各マウス移動で、ポリゴンとエッジのインデックス構築や候補探索を毎回やり直し、操作が著しく重くなった。
*   **原因**: 高頻度に呼ばれる処理で、不変データ（ポリゴン一覧、頂点→ポリゴン索引、レイヤー別の候補など）を都度再計算していた。
*   **教訓**:
    *   ドラッグのような高頻度処理では、変化する入力と不変な前処理を分離し、不変データは操作単位で事前計算して使い回す。
    *   キャッシュは操作のライフサイクルに合わせて破棄し、操作外へ持ち越してデータの鮮度を損ねない。

### 8.12.1 ビューポート変更で全地物を再描画して性能が劣化した
*   **問題**: パン/ズームのたびに `SVGRenderer.render` を呼び出し、全地物のDOM再生成が発生して操作が固まった。
*   **原因**: ビューポート変更イベントが常にフル描画をトリガーし、viewBox更新と地物再描画を分離していなかった。
*   **教訓**:
    *   高頻度に変化するビューポート操作では、viewBox/背景/グリッドの更新と地物再描画を分離し、全地物の再生成を操作終了後に集約する。
    *   連続入力（ホイール・ドラッグ）中の再描画は、デバウンスでまとめて最終状態のみを確定描画する。

### 8.13 履歴アンカーの上書きでプロパティが消失した
*   **問題**: プロパティ編集で新しい `Property` だけを渡し、既存の `feature.properties` を丸ごと置き換えたため、過去/未来の歴史の錨が消失した。
*   **原因**: `UpdateFeatureUseCase` と `EditingViewModel.updateFeatureProperties` が `properties.length === 1` を前提にし、View が単一要素配列で更新していた。
*   **教訓**:
    *   履歴を表す配列は「全量更新か差分更新か」を明示し、UI/UseCase の境界で既存配列の保持を必須にする。
    *   既存配列に統合する必要がある更新では、更新対象の特定規則（時間点の一致など）をコードで一箇所にまとめ、上書きによる履歴消失を防ぐ。

#### 8.13.1 錨分割時に旧錨の終了時刻を切らず過去編集が失敗した
*   **問題**: 未来時刻で先に錨を追加した後、過去時刻を編集すると「存在終了は次の歴史の錨の開始時刻を超えられません」で保存に失敗した。
*   **原因**: `UpdateFeatureUseCase` が新しい錨を追加する際、編集元の旧錨の `endTime` を編集時刻で打ち切らずに残したため、過去時刻編集画面が古い `endTime`（未来錨より後）を初期値として保持し、次回保存時の境界検証に違反した。
*   **教訓**:
    *   時間点で履歴錨を分割する処理では、新しい錨の追加だけでなく、分割元の錨の有効期間（`endTime`）も同時に更新して、錨同士の時間境界を常に正規化する。
    *   手動テストと自動テストは「未来側編集→過去側編集」の順序も必ず含め、単一方向の編集だけで成立する擬似的な正常系に依存しない。

#### 8.13.2 履歴アンカー競合解決で複数地物更新を単一履歴にしないと Undo/Redo が破綻する
*   **問題**: 競合解決で編集対象以外の地物の履歴も同時更新されるのに、履歴コマンドを単一地物向けのまま記録すると Undo/Redo で片側だけ戻り、時間整合が崩れた。
*   **原因**: `EditingViewModelOperations.updateFeatureProperties` が `UpdateFeatureUseCase` の戻り値から編集対象地物しか履歴化せず、副作用で更新された地物を履歴ペイロードに含めていなかった。
*   **教訓**:
    *   競合解決や連鎖更新のように複数地物へ副作用が出る処理では、UseCase 戻り値に更新対象の全集合を明示し、履歴はバッチコマンドで単一操作として記録する。
    *   競合解決UIは「全競合の方針指定完了」まで確定を無効化し、キャンセル時は再試行せず操作全体を失敗として中断する。

#### 8.13.3 時刻付き形状の参照時刻を統一しなかった
*   **問題**: タイムラインを過去時刻に移動すると、表示中の頂点をクリックしても選択できず、近接判定も最新形状を参照して誤作動した。
*   **原因**: `SVGRenderer` は `currentTime` で `getRingsAt/getVertexIdsAt/getVertexIdAt` を使っていた一方、`MapViewInteractionLogic`・`MapViewModelSelection`・`MapViewRendererHelper` が `feature.rings` / `feature.vertexIds` / `feature.vertexId`（時刻未指定=最新アンカー）を参照していた。
*   **教訓**:
    *   時刻依存の地物形状を扱う処理では、描画・選択・近接判定・ハイライトで同じ `currentTime` を受け渡し、参照経路を統一する。
    *   `Feature` の生配列 (`rings` / `vertexIds`) を直接参照せず、時刻付きアクセサ（`getRingsAt` など）を利用するヘルパーを用意して重複実装を避ける。
    *   回帰防止として、「過去時刻の頂点が選択できる」ケースを `tests/presentation/viewModels.test.js` と `tests/presentation/mapViewInteractionLogic.test.js` に固定する。

#### 8.13.4 履歴編集の更新契約が Property 投影へ逆流した
*   **問題**: 履歴アンカー編集・削除・Undo/Redo の更新契約が古い `Property` 配列を入力として扱い続け、`FeatureAnchor` 正準モデルで統一されなかった。
*   **原因**: `PropertiesTabView`・`EditingViewModelOperations`・`UpdateFeatureUseCase`・`UpdatePropertiesCommand` が `feature.properties` をそのまま更新ペイロードに使い、`withProperties` を経由していた。
*   **教訓**:
    *   `Feature.properties` は読み取り専用投影として扱い、更新ペイロードは `FeatureAnchor` 配列へ正規化してから UseCase に渡す。
    *   `UpdateFeatureUseCase` と履歴コマンドで `updates.properties` / `oldProperties` / `newProperties` を明示的に拒否し、互換書き込み経路を残さない。
    *   履歴コマンドは `oldAnchors/newAnchors` が空のときに失敗させ、空配列を「空更新」として通さない。
    *   分割・競合解決・履歴復元のような別導線でも `withAnchors` を共通入口にして、例外経路で旧更新モデルへ戻らないようにする。

#### 8.13.5 ドメイン生成契約が Property 互換へ逆流した
*   **問題**: アプリケーション層・永続化層では `anchors` 必須契約へ移行済みなのに、`Feature` / `Point` / `Line` / `Polygon` が `Property` 配列から暗黙にアンカーを再構築できる状態を残し、層ごとの契約が分裂した。
*   **原因**: エンティティコンストラクタと `withProperties` が互換経路を保持し、`anchors` 欠落時に旧データモデルへフォールバックしていた。
*   **教訓**:
    *   不変条件（今回なら「地物は必ず `anchors` を持つ」）はドメインエンティティ自身で強制し、上位層だけでの入力検証に依存しない。
    *   旧形式からの変換が必要な場合は、本番コードのエンティティ内部に残さず、移行処理やテスト補助へ責務を隔離する。
    *   `withProperties` / `addProperty` のような旧更新導線は例外で停止させ、失敗を明示して再発を防ぐ。

#### 8.13.6 編集時刻付き頂点削除で競合解決を通していなかった
*   **問題**: 編集時刻付きの頂点削除でポリゴン形状が変化した際、同一レイヤー面情報の重なりが発生しても保存できてしまい、「2.2.3 編集時の時間的・空間的整合性維持プロセス」に反していた。
*   **原因**: `VertexEditUseCase._deleteVerticesAtTime` が `resolvePolygonAnchorConflictsOrThrow` を呼ばずに `saveWorld` しており、`EditingViewModelOperations.deleteVertices` と `DeleteVerticesCommand` も `conflictResolutions` の受け渡し契約を持っていなかった。
*   **教訓**:
    *   履歴アンカーを更新する全導線（属性編集・頂点移動・頂点削除・分割）で、保存前の整合性フェーズ（競合検出→解決方針→確定）を同一契約で実装する。
    *   競合解決を伴う操作は、Redo を含めて同じ解決方針を再適用できるよう履歴ペイロードに解決方針を保持する。
    *   競合エラーで失敗する可能性のある UseCase は、`saveWorld` 前に例外が発生した場合でも `world` キャッシュをロールバックし、失敗時のメモリ状態破損を防ぐ。

## 9. コード品質と保守性

### 9.1 コード品質指標

- **複雑度**: メソッドの循環的複雑度を10以下に抑える
- **重複度**: 重複コードは5%未満に抑える
- **コメント率**: 重要なクラス/メソッドには適切なコメントを記述
- **エラー処理**: すべての例外が適切に処理されていること

### 9.2 保守性向上策

- **コンポーネント分離**: 責務を明確に分離し、コンポーネント間の結合を最小化
- **依存性逆転**: インターフェースを介した依存関係の管理
- **コーディング規約**: 一貫したコードスタイルの適用
- **ドキュメント化**: アーキテクチャ、設計決定、APIを文書化
- **継続的リファクタリング**: 技術的負債の早期解消

## 10. まとめ

時空地図アプリケーション（time-map-app）の開発は、ドメイン駆動設計とクリーンアーキテクチャの原則に基づいて進めます。主要な課題は、共有頂点システム、レイヤー階層構造、領域排他性、時間依存データ管理の実装です。

開発は段階的に進め、各フェーズで特定の機能セットを完成させます。コード品質と保守性を維持するため、適切なテスト戦略とコーディング規約を適用します。

このガイドに従うことで、機能的で信頼性の高い、そして将来の拡張にも対応できるアプリケーションを構築できるでしょう。

