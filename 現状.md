確認されている不具合：
以下が、現時点で修正されていない主な不具合のリストです：

## 未解決の不具合リスト

### 重要度高（機能的問題）

1. **設定機能が軒並み未実装**
   - 赤道長の設定などの、世界全体に影響するカスタムが提供されていない。
   - ボタンすらない。
   - 要件には記載済みなので単なる未実装

2. **移動時、頂点がリアルタイムに追従しない** 
   - ドラッグ中はリアルタイムに位置が変動するべき
   - この時、線情報または面情報に属するものであれば、線の描画も更新されてほしい
   - 現状実装されていない排他処理や共有頂点にも対応するコーディングが求められる

3. **時間変化に関する機能が軒並み未実装**
   - 同じ地物の形状変化や、プロパティの経時変化など
   - 大きな改修が必要になるかも。心して実装するように。

4. **コンテキストメニューが全て未実装**
   - コンテキストメニューの仕様は存在するので、単なる未実装
   - 右クリックでコンテキストメニューを開こうとした時、それを検知した旨のアラートは出る。

5. **地物のループ描画が未実装**
   - 未実装。

6. **面情報レイヤー、排他処理、共有頂点の未実装**
   - 難しい実装かつ、目玉機能と言っていいような特別な機能なので、心して実装するように。
   - 仕様は詳細に書いたが、まだ不完全かもしれない。
   - 現状の実装や仕様と衝突するところが多いかも？
   - 場合によってはこれらの機能の追加を想定した造りになっていないことを問題点として挙げないといけないかも

7. **穴あけツールで穴あけをした後、能動的な保存を行うと穴が消える**
   - 現状、能動的に保存を行うにはプロパティ編集からの保存ボタンを利用するのが一番確実
   - これで穴を開けた地物を編集して保存すると、穴が消える

### 重要度中（UI/UX問題）

1. **編集ツールでの視点移動方法がほしい**
   - 候補A：WASDで移動
   - 候補B：中クリックで移動
   - 候補C：一旦後回し
   - どれにせよ、仕様に記載が必要

2. **穴あけツールで穴あけをしたあと、表示ツールに戻ってしまう**
   - 連続で穴を開けたい可能性があり、穴あけツールのままであるべき。
   - ついでに選択状態もリセットされてしまうので、これも維持してほしい

3. **ツールの実装が不完全**
   - 移動・分割ツールが機能していない
   - 詳細な動作を仕様書に書き込む必要あり

4. **地物の表示が微妙に見づらい**
   - 良い表示方法を考える必要がある
   - ラベルの視認性（小さい）や、地物の色（地図に同化する・同じ種類の情報は同じ色でいいのか？）など
   - 穴開きの面領域で、穴がちょうど文字に重なると穴の中に名前が表示されてしまう。名前は面情報の中にあってほしい
   - これは地図そのものの表現方法などの難しい問題なので、優先度は低い
   - 詳細な動作を仕様書に記載したい

5. **測定ツールの測定結果が見づらい**
   - 大円距離を数値で表示するなら、大円距離での線を描画するべき
   - この不具合を修正した場合、仕様に挙動を書き加えること。

6. **Ctrl+Rでリロードした際、グリッドの表示がおかしくなる**
   - 全画面表示にした上でリロードすると問題が生じる
   - 地図の描画領域の左右の端に、グリッドが描画されていない場所が発生する。
   - 上下は問題ないので、正方形状になってしまっている？
   - デフォルトの画面サイズに影響されているのかも
   - 全画面表示しない状態でリロードすると正常に出る。
   - 不具合発生中にデフォルトの大きさに戻すと、再度全画面にしてもグリッドが正常に表示される
   - でもリロードすると再現性高く発生する

7. **deleteキーによる頂点削除が効いていない**
   - deleting selected vertices:というログは出るので、検知はできている
   - そういえば共有頂点とかどうするのか決めないといけない

8. **パン中にレイヤー一覧などの文字に選択が効いてしまうのがよくない**
   - 連続でパンをしようとすると、選択された文字のドラッグが優先されてしまう場合がある
   - かといって文字列選択自体できないとレイヤー名をコピーしたいときなどに困る

9. **地物一覧のカテゴリが見づらい**
   - 開発中なのでdefaultなどが頻発するせい？
   - できればまず点情報・線情報・面情報で分類してから、その中で属性を別に表示してほしい
   - カテゴリではなく属性とか分類と呼んでほしい

### 重要度低（拡張機能）
1. **セーブ・ロード機能が未実装**
   - ボタンは追加されたが機能は未実装
   - 現状は自動保存のみ機能

2. **パフォーマンス問題**
   - ちょっと重たい
   - ログ出力のせいかもしれないので一通り機能が落ち着くまで様子見

3. **余計なタッチ機能の中途半端な実装がある**
   - タッチ対応を中途半端に実装するのはコードを無駄に大きくするので害になる
   - タッチ対応はまだ実装するべきではない

4. **「面情報の境界線上または線情報に点を追加するツール」がない。**
   - 追加ツールを選択した状態で、面情報の境界線（または線情報）上の任意の場所をドラッグすると、そこに新しい頂点が追加される。
   - 追加されるだけではなく、スムーズにドラッグして頂点を移動することが必要。
   - クリックを離すことで頂点追加が確定。
   - 修正時、仕様に記載する必要あり
