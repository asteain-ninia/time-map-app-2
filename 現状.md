確認されている不具合：
以下が、現時点で修正されていない主な不具合のリストです：

## 未解決の不具合リスト

### 重要度高（機能的問題）

1. **設定機能が軒並み未実装**
   - 赤道長の設定などの、世界全体に影響するカスタムが提供されていない。
   - ボタンすらない。
   - 要件には記載済みなので単なる未実装

2. **欠番**  

3. **欠番**

4. **欠番**

5. **欠番**

6. **オブジェクト選択や編集が全くできない**
   - あらゆるオブジェクトの頂点ハンドルが表示されない
   - なんか未実装っぽい？
   - 地物表示の問題（中-4）と関連する。
   - 見やすい表示方法を検討し、仕様に記載する必要がある


### 重要度中（UI/UX問題）
1. **編集ツールが全て未テスト**
   - ボタンが反応するのは確認済み

2. **「元に戻す」と「やり直し」機能が未テスト**
   - 一部は未実装？
   - なんとなくもとに戻すは機能してるっぽい

3. **緯度経度の表示がおかしい**
   - ズームするほど緯度経度の数字表示が巨大化する
      - 主に経度の表示が重なって読めない。100以上で百の位が/Eと重なってしまう
      - 逆にズームが引きだと小さくなって読めない。
      - ズームにかかわらず常に一定の大きさで表示されてほしい
   - この不具合を修正した場合、仕様に挙動を書き加えること。

4. **地物の表示が見づらい**
   - 追加後の線情報と、面情報の境界線が細すぎる
   - ズーム倍率にあった太さになってほしい。画面上の相対的なサイズが一致するように。
   - この不具合を修正した場合、仕様に挙動を書き加えること。

5. **測定ツールの測定結果が見づらい**
   - 大円距離を数値で表示するなら、大円距離での線を描画するべき
   - この不具合を修正した場合、仕様に挙動を書き加えること。

6. **Ctrl+Rでリロードした際、グリッドの表示がおかしくなる**
   - 全画面表示にした上でリロードすると問題が生じるっぽい

7. **地図の端に白い線が表示される**
   - main-groupクラスのg要素の下のg要素の下のg要素3つの間に小さな隙間が空いている。
   - もとのSVGに端っ子に問題がないことは確認済み

8. **パン中にレイヤー一覧などの文字に選択が効いてしまうのがよくない**
   - 連続でパンをしようとすると、選択された文字のドラッグが優先されてしまう場合がある
   - かといって文字列選択自体できないとレイヤー名をコピーしたいときなどに困る

9. **地物一覧のカテゴリが見づらい**
   - 開発中なのでdefaultなどが頻発するせい？
   - できればまず点情報・線情報・面情報で分類してから、その中で属性を別に表示してほしい
   - カテゴリではなく属性とか分類と呼んでほしい

### 重要度低（拡張機能）
1. **セーブ・ロード機能が未実装**
   - ボタンは追加されたが機能は未実装
   - 現状は自動保存のみ機能

2. **パフォーマンス問題**
   - ちょっと重たい
   - ログ出力のせいかもしれないので一通り機能が落ち着くまで様子見

3. **余計なタッチ機能の中途半端な実装がある**
   - タッチ対応を中途半端に実装するのはコードを無駄に大きくするので害になる
   - タッチ対応はまだ実装するべきではない

4. **「面情報の境界線に点を追加するツール」がない。**
   - 追加ツールを選択した状態で、面情報の境界線上の任意の場所をドラッグすると、そこに新しい頂点が追加される。
   - 追加されるだけではなく、スムーズにドラッグして頂点を移動することが必要。
   - クリックを離すことで頂点追加が確定。
   - 修正時、仕様に記載する必要あり

5. **地物追加時のダイアログ・プロパティ編集に入力ができなくなることがある**
   - 詳細不明
   - 開発者ツールからダイアログのinput要素を選択すると、入力できるようになることがある
   - ダイアログは地図の上にでるので、クリックイベントに問題があるかも