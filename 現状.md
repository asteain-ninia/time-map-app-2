確認されている問題：
以下は、現時点で確認されていて修正されていない主な問題のリストです：

## 重要課題と進捗

### 重要度高（機能的問題）

1. **[完了: 2026-02-16] 履歴アンカー編集時の「2.2.3 編集時の時間的・空間的整合性維持プロセス」におけるデータモデル未接続**
   - [完了] 根本原因（サブ原因2）: 分割専用だった既存 `ConflictResolutionDialog` と責務を分離し、履歴アンカー編集用の競合解決UIを接続済み。
   - [完了] 根本原因（サブ原因3）: `FeatureAnchor` 値オブジェクトを導入し、`timeRange + property + shape + placement` を一体で保持する正準モデルへ移行。
   - [完了] `Feature.properties` を履歴アンカー由来の読み取り専用投影として扱い、編集・保存・履歴・描画の書き込み元を `FeatureAnchor` へ集約。
   - 補足: 時系列空間競合検出（`i=0` を含む）は保存時検証に統合済み。競合解決は段階実装として「非優先地物の存在期間打切り」で接続済み。

2. **スタイル改善が未実装**
   - 仕様書にも未記載
   - スタイル改善案.mdに改善案の詳細あり
   - 古い改善案docになっているので、着手時検証が必須

3. **時間変化に関する機能が軒並み未実装**
   - 大きな改修が必要になる。心して実装するように。
   - 仕様は要件定義書にて詳細に書いたが、まだ不完全かもしれない。追加が要るか検討するように。
   - [完了: 2026-02-17] 編集時刻つき頂点移動（Point/Line/Polygon）で、履歴アンカーの時点分割・未来アンカー保持・Undo/Redo一体復元を実装。
   - [完了: 2026-02-17] 上記に対応する手動テスト項目（既存アンカー時刻更新、複数頂点移動、自己交差失敗時の非保存/非履歴化を含む）を自動テストへ反映。
  - 残作業:
     - `Property` 縮退の未完了項目（時間・形状・所属責務の完全除去）を完了させること（項番7）。
     - 時間変化機能のE2E観点（タイムライン操作＋編集＋履歴復元）の手動確認手順を定例化すること。

4. **[完了: 2026-02-13] タスクA（設計固定）: 時間編集の保存契約とデータモデルを確定**
   - 実施内容:
     - 履歴アンカー単位で「属性だけ編集する場合」と「形状まで編集対象になる場合」を要件定義書で明文化。
     - 錨ごとの形状保持方式を `FeatureAnchor` 正準へ固定し、`timeRange + property + shape + layer/親子関係` の一体保持を定義。
     - `Feature.properties` を `FeatureAnchor` 由来の読み取り専用投影として定義し、直接書き込み禁止を明示。
     - 競合解決の入出力契約（Prepare/Resolve/Commit の3段階）を要件定義書に追記。
     - 保存形式の互換方針を「互換なし（旧形式自動移行なし）」で確定。
   - 完了判定:
     - 要件定義書の仕様本文・内部実装補遺の双方で反映済み。

5. **[完了: 2026-02-17] タスクB（保存前検証導線）: 錨衝突バリデーション＋競合検出フェーズを保存前に追加**
   - 目的: 錨衝突バリデーションと競合解決フェーズの保存前統合を完了する。
   - 作業:
      - `updateFeatureProperties` の導線を「編集案生成 -> 錨衝突バリデーション -> 空間競合検出 -> 競合解決 -> 確定保存」に再編する。
        - 時系列空間競合検出（`i=0` を含む）は実装済み。
      - [完了] 同一時刻アンカー重複・期間矛盾の検証責務を `UpdateFeatureUseCase._normalizeAndValidateAnchorTimeline` に集約。
      - [完了] `propertyEdit` / `anchors` の双方が同一の保存前検証を通る導線へ統合。
      - [完了] 検証失敗時に保存を確定しないこと（`saveWorld` 未実行）をテストで固定。
      - [完了] 検証失敗時に履歴コマンドを積まないこと（`EditingViewModelOperations.updateFeatureProperties`）をテストで固定。
      - [完了] 手動確認手順（重複複製/保存失敗表示/失敗操作の履歴非記録/Undo）の主要フローを自動テストへ反映。
   - 完了条件:
      - [完了] 保存直前検証を通らない更新は永続化されない。
      - [完了] テストで錨衝突ケースが再現・防止できる。

6. **[完了: 2026-02-14] タスクC（競合解決UI接続）: 履歴アンカー編集用の競合解決ダイアログを接続**
   - 目的: サブ原因2を解消し、要件2.2.3の「ユーザー選択後に確定」を成立させる。
   - 実施内容:
     - [完了] 分割専用ダイアログと責務を分離し、履歴アンカー競合用UI（競合一覧、優先側選択）を実装。
     - [完了] 全競合に解決方針が設定されるまで確定不可にする制約を実装。
     - [完了] 解決適用で複数地物が更新される場合も単一履歴操作（Undo/Redo 1回）として記録。
   - 完了条件:
     - [完了] 競合あり編集でダイアログ経由の確定フローが機能する。
     - [完了] キャンセル時に部分保存が発生しない。

7. **[進行中: 2026-02-16] タスクD（段階移行）: Property 縮退を独立実施**
   - 目的: `FeatureAnchor` 正準モデルへの移行実装を完了し、旧 `Property` 書き込み経路を廃止する。
   - 作業:
     - [完了] `Feature.properties` への直接書き込みを廃止し、`FeatureAnchor` 由来の読み取り専用投影へ統一する。
     - [完了: 2026-02-17] 履歴編集の更新契約（`PropertiesTabView` / `EditingViewModelOperations` / `UpdateFeatureUseCase` / `UpdatePropertiesCommand`）を `FeatureAnchor` 正準へ統一した。
     - [完了: 2026-02-17] `EditingViewModelOperations` の履歴ペイロードを `oldAnchors/newAnchors` に統一し、Undo/Redo コマンド経由の更新契約を `anchors` 優先へ固定した。
     - [完了: 2026-02-18] `UpdateFeatureUseCase` と履歴コマンドの `Property` 互換書き込み経路（`updates.properties`, `oldProperties/newProperties`）を撤去し、`FeatureAnchor` 配列以外の更新入力を拒否する契約へ固定した。
     - `Property` から時間・形状・所属の責務を段階的に除去し、属性スナップショットに限定する。
     - [未着手] 後者の `Property`（`Feature.properties` / `PropertyProjection` / `Property` クラス互換経路）を段階移行完了後に痕跡ごと完全撤去する（UI・ユースケース・履歴・保存・テスト補助を含む）。
     - [完了] JSONシリアライザ、履歴シリアライザ、レンダラー、時間移動ロジックを `FeatureAnchor` 正準へ移行する。
     - [完了] 旧 `Property` 保存形式との後方互換は実装しない。
   - 完了条件:
     - 編集・保存・履歴・描画で `FeatureAnchor` が唯一の書き込み元となる。
     - `Property` 縮退前のロジックが残っていない。

8. **面情報レイヤー概念の未実装**
   - 難しい実装かつ、目玉機能と言っていいような特別な機能なので、心して実装するように。
   - 仕様は詳細に書いたが、まだ不完全かもしれない。
   - 現状の実装や仕様と衝突するところが多いかも？
   - 大きな改修が必要になる。心して実装するように。

9. **要件定義書5.3.2を消さないといけない**
   - 「現行では～」等といった永続化するべきでない情報を要件定義書に含められてはいけない。この旨の追記含め削除すべし。
   - 他にも要件定義書に落書き・齟齬・更新不足がないか確認が必要。

### 重要度中（UI/UX問題）

1. **開発ガイドが現状に即していない**
   - 時代遅れ・実践されていない記述が頻発している
      - ブランチ規則**とか**色々
   - 教訓も少し多すぎるかもしれない。統合・整理が必要

2. **ツールの実装が不完全**
   - 分割ツールは外周リング1つの面情報のみ対応（穴・飛び地・下位領域は未対応）
   - 分割ツールのテストが導入されていない！

3. **共有頂点がつくりづらい**
   - ：つかみカーソルに頂点が隠れてしまう
      - 辺のスタイルに変化を起こすことで表示の代わりにしたい
      - 要件定義書に記載済み
   - 近づいたところからドラッグ開始したら共有頂点化しないという仕様は失敗だった。消したい。近くからドラッグ開始しても共有頂点化してほしい。

4. **ラベル表示の仕様が一部未実装**
   - 主に点・線情報の仕様
   - 面情報は実装済み。

5. **余計なコメントを消したい**
   - 主にGeminiが残したーー変更ここからーー的なやつ
   - あとは～から変更みたいなのも消したい。コメントは変更履歴であるべきではない。　
   - まだ残ってるか要調査

6. **プロパティの編集ができなくなる時がある**
   - サイドバーでプロパティを編集したあと、プロパティにフォーカスが合わせられなくなる
   - でも選択は出来るし、削除も出来る。しかしカーソルが出ないためキーボード入力が不能。
   - アプリ自体のフォーカスを一旦外すと選択 **できるようになる**。この状態から一回外してもフォーカス可能。
   - アプリから離れずとも、開発者ツールを触っても選択できるようになる。
   - 何度も修正に失敗した難しい不具合なので優先度最低
      - もしかしたらelectron側の問題かも？
      - この不具合を優先して直そうとした場合、怒ります。

7. **一部のファイルが巨大になってしまっている**
   - 巨大なファイルは多くの場合重要であるため、頻繁に変更もされる
   - AIの扱える長さに限界があるため、うまく出力できなくなる原因になる
   - 最高でも1000行以下に抑えたい
   - デグレがものすごく非常にとんでもなく怖いので、テストなどで対策しながら作業すること

8. **プロパティ継承先選択モーダルの改善が必要**
   - 非常に見づらい。線が太すぎる。
   - 仮の名前をつけてラベルを表示するべき
   - ボタンもプレビューよりも外側にあってほしい
   - なぜ背景は映してくれないのか
      - 全体的にメイン地図の表示と共通化したほうが良くない？
      - 錨の衝突UIでも使うはず。

9. **頂点滑りの改善が必要**
   - 他領域と交差してしまう場合（他領域と交差する形状を作ることになる境界辺）にはスナップしてほしくない
   - 滑る時、カーソルと離れすぎる。
      - アルゴリズムの変更が必要

10. **ロード直後に地物ラベルが見えなくなる**
   - 地物一つを選択すると全部見えるようになるので機能自体へのダメージなし

11. **完了不具合の記載を消したい**
   - 番号が解決した不具合がリストの最後なら、項番ごと消す
      - 消した際、欠番が最後になる場合、その欠番を消す
   - 最後でなければ欠番にする
   - 不具合の履歴は（残すべきものの場合）開発ガイドにあるべきで、現状.mdは今ある不具合のみに絞られるべき
   - このルールを明文化するべき

### 重要度低（拡張機能）

1. **セーブ・ロード機能が未実装**
   - ボタンは追加されたが機能は未実装
   - 現状は自動保存と、プロパティ編集からの保存ボタンでの保存呼び出しだけが機能している
   - 複数ソースからのロード・オートセーブと任意セーブの分離・任意に名前をつけて保存する機能など、ほしいが要件に書きそこねた要素がかなりある
      - これをヒアリングしながら要求仕様として固める必要がある

2. **最適化を進めたい**
   - 一応ものすごく重たい状況はなくなった
   - しかし、今後もスケールできるかは不透明
   - もっと抜本的な最適化が欲しいかも？

3. **地物の表示が微妙に見づらい**
   - ラベルは飛び地を含む領域ごとに内部余裕点へ配置されるようになったが、フォントサイズやカテゴリ別の装飾など視認性を高めるデザイン検討は継続課題。
   - 地物の配色やハイライトが機能ごとにバラつき、背景とのコントラストが不足するケースがある。テーマ設定や色覚バリアフリー対応を含めた整理が必要。
   - 選択状態の頂点表示・編集ハンドルのスタイルが統一されておらず、複数ツール使用時に情報過多になる。視覚ヒエラルキーの見直しを行いたい。
   - これは地図そのものの表現方法などの難しい問題なので、優先度は低い。
   - 詳細な動作を仕様書に記載したい。

4. **テストが正しいか確信できない**
   - AI任せで作ったので、レビューできていない
   - テストがおかしくないかを継続的に検証し続けることが必要。
   - テスト問題点.mdに以前の分析を記載済み。直すときはこれがまだ通用するかも見ないといけない。
   - めんどくさいので最近手動テストを網羅的にやっていない。デグレ・新機能含めて全機能の基本動作とコーナーケースの検証を含んだ手動テストリストが欲しい
      - もちろん、自動テストでも同じテストはしていて欲しい。機械的テストと手動の人間テストのダブルチェック

5. **TimePointとTimeRangeが併存している**
   - これどっちかで良くない？
   - ２つになった経緯を忘れた。TimeRangeの導入がTimePointよりも遅かった気がするので、単にTimePointが古いだけかも
   - TimePointがレガシーなら、消したい
   - TimePointが必要なら（意味を持たせられるなら）仕様に記載する必要がある

6. **テストのjsdom対応**
   - 手動テストの自動化が出来るようになったはず
   - まだ歴史の錨の少ししか対応していないはず。
   - 他、基本的な機能も含めてデグレ確認出来るようにするべき
   - すごく大変そうなので計画立てて残り作業をココに追記していく形で作業してもよし

7. **にほんごをつかいたい**
   - **わかりづらくならない範囲**で、日本語の単語を使いたい
   - 例えば：「アンカー」→「歴史の錨」　「プロパティ」→「地物詳細」
   - わかりづらくならない範囲というのが大事

### 重要度最低（未顕在の不具合）

最低重要度の問題を単体で修正することは原則として禁止する。

1. 共有頂点に関する未実装起因の不具合（？）
   - 共有頂点のGUI操作（ドラッグによるスナップ等）が未実装であるため、現在は顕在化していないが、コード上以下の不具合の存在が確認されている。
     - 複数レイヤーにまたがる共有頂点の選択解除問題: 異なるレイヤーに属する地物間で頂点を共有している状態で、片方のレイヤーを非表示にすると、表示中の地物が使用しているにもかかわらず、共有頂点の選択が解除されてしまう。
     - 共有頂点の解除: 共有を解除した結果、ポリゴンが自己交差する不正な形状になった場合でも、エラー検知の前にデータが保存されてしまう。
     - 複数頂点のドラッグ: 複数頂点のドラッグ中に、一部の頂点がスナップした場合、他の頂点の移動量の計算にスナップが考慮されず、頂点間の相対位置が崩れる。
   - **以上の不具合は共有頂点実装前に記載されたもの（現在は実装済み）だが、直っているか未検証**
      - 直っているかの検証を行うことは認める

2. **設定ファイル経由で独自歴を有効化すると破綻する**
   - 現行実装では設定ファイルから `calendar.daysPerMonth` 等を変更できるが、`TimeService._getMonthDays` が常に「インデックス1=2月」とみなしてうるう年加算するため、日数の合計が狂い、`TimelineViewModel` など月日クランプ処理が誤動作する。
   - UI上の独自歴設定は未提供でも、設定を書き換えた途端に `advanceDays`、`getDaysInMonth`、日付入力のバリデーションが破綻する。
   - 現状は設定ファイルを直接編集した場合にのみ発生し、UIから独自歴を切り替える導線も未実装なので、正式に独自歴を扱うフェーズで `src/domain/services/TimeService.js` のうるう日決め打ちを含めて改修する方針とする。

3. **内部的に非選択なのに見かけ上選択状態になることがある気がする。**
   - 今のところ乖離することはないはずだが、他不具合次第で顕在化するかも
   - どうも内部の選択状態と選択状態の表示が直結していないせい？
