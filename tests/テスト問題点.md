# テスト問題点メモ

## ManageLayersUseCase に関する検証漏れ
- 階層検証失敗時のテスト (`tests/application/manageLayersUseCase.test.js`)
  ```js
  it("throws when layer hierarchy validation fails on add", async () => {
    layerService.validateLayerHierarchy.mockReturnValueOnce(false);

    await expect(useCase.addLayer("Invalid")).rejects.toThrow("Layer hierarchy validation failed");
    expect(worldRepository.saveWorld).not.toHaveBeenCalled();
  });

  it("does not save when reordered layers break hierarchy", async () => {
    world.layers.push(new Layer("layer-1", "Overlay", 1, true, 1.0));
    world.layers.push(new Layer("layer-2", "Top", 2, true, 1.0));
    layerService.validateLayerHierarchy.mockReturnValueOnce(false);

    await expect(useCase.reorderLayer("layer-2", 0)).rejects.toThrow("Layer hierarchy validation failed");
    expect(worldRepository.saveWorld).not.toHaveBeenCalled();
  });
  ```
  例外と `saveWorld` 呼び出しの有無のみをチェックしており、`world.layers` の件数や順序が汚れていないかを確認していない。失敗後に配列が変化してもテストは合格する。
- 問題が顕在化するテストケース:
  - `tests/application/manageLayersUseCase.test.js` の `it("throws when layer hierarchy validation fails on add")`
  - 同ファイルの `it("does not save when reordered layers break hierarchy")`
- 直接の原因となる実装箇所:
  - `src/application/usecases/ManageLayersUseCase.js` 内の `addLayer`（検証前に `push`）
  - 同ファイル内の `reorderLayer`（検証前に `world.layers = updatedLayers`）

- 実装側の状態変更 (`src/application/usecases/ManageLayersUseCase.js`)
  ```js
  world.layers.push(newLayer);
  const isValid = this._layerService.validateLayerHierarchy(world.layers);
  if (!isValid) {
    throw new Error("Layer hierarchy validation failed");
  }
  ```
  ```js
  world.layers = updatedLayers;
  const isValid = this._layerService.validateLayerHierarchy(world.layers);
  if (!isValid) {
    throw new Error("Layer hierarchy validation failed");
  }
  ```
  検証前に `world.layers` を直接書き換えており、失敗時にロールバックする処理が存在しない。テストではレイヤー数・順序が変わっていないことを明示的に確認しないと、既知の不具合を見逃す。

- 具体的な破綻シナリオ  
  `useCase.addLayer` による追加では、`layerService.validateLayerHierarchy` が `false` を返した瞬間に `world.layers` へ追加済みの `Layer` インスタンスが残留する。`ManageLayersUseCase` は `worldRepository.getWorld` の結果をそのまま破壊的に扱っているため、テストが `world.layers` の件数をチェックしないままだと、再試行時に `order` 衝突やダブり ID を誘発する。`useCase.reorderLayer` でも同様に `updatedLayers` を代入した後で例外を投げるため、`layerId` の順序が途中で固定され、以降の操作結果が不定になる。これらを検出するには、テストで `expect(world.layers).toEqual([...元の配列...])` のような状態比較や、`world.layers.map((l) => l.id)` の順序比較を追加する。
- 追加で検証したい観点  
  1. 失敗後の `world.layers.length` および `world.layers.map((l) => l.order)` が呼び出し前と一致しているか。  
  2. 例外後に `layerService.validateLayerHierarchy` が再実行された際、`world.layers` が再利用されることによる副作用がないか。  
  3. `layerService.validateLayerHierarchy` を `mockReturnValueOnce(false)` の後に `mockReturnValue(true)` へ戻して再呼び出しし、追加・並べ替えが正常に完了することを確認する再試行テスト。

## プレゼンテーション層のプライベート依存
- 内部ヘルパーを直接呼び出すテスト (`tests/presentation/mapViewRendererHelper.test.js`)
  ```js
  const result = helper._unwrapLongitudeSequence(sequence);
  ```
  `_unwrapLongitudeSequence` はアンダースコア付きの内部ヘルパーであり、公開 API ではない。メソッド名や公開範囲をリファクタリングすると、機能仕様に変化がなくてもテストが壊れる。
- 該当テストケース:
  - `tests/presentation/mapViewRendererHelper.test.js` の `it("returns the same coordinates when longitudes stay within +/-180°")`
  - 同ファイルの `it("adjusts longitudes that wrap from +180° to negative values")`
  - 同ファイルの `it("adjusts longitudes that wrap from -180° to positive values")`

- 該当メソッドの定義 (`src/presentation/views/map/MapViewRendererHelper.js`)
  ```js
  _unwrapLongitudeSequence(points) {
    if (!Array.isArray(points) || points.length === 0) {
      return [];
    }
    const result = [{ x: points[0].x, y: points[0].y }];
    let previousLongitude = points[0].x;
    let wrapOffset = 0;
    ...
  }
  ```
  private 的に扱われているヘルパーで、テストから直接参照する設計になっていない。公開メソッド経由で描画結果や座標列を検証するテストに置き換える必要がある。
- 依存しない形での代替案  
  - `renderDistanceMeasurement` や `renderGreatCircleRoute` など `_unwrapLongitudeSequence` を内部使用している公開メソッドを利用し、描画対象の座標列が期待通りアンラップされるかを確認する。  
  - あるいは `MapViewRendererHelper` に座標アンラップ結果を返す公開ユーティリティを導入する設計変更を検討し、テスト側はその公開契約に依存する。現状は private 実装を固定化しているため、ユーティリティの引数/戻り値が変わるとテスト全面差し替えになる。

## MapViewEventHandler のキーボード操作が未検証
- DOM イベントハンドラである `MapViewEventHandler` について、Vitest 等での単体テストが存在しない。今回の ESC 処理修正でも、挙動確認は手動検証に頼らざるを得なかった。
- 該当実装 (`src/presentation/views/map/MapViewEventHandler.js:479-500`)
  ```js
  const activeFeatureId = this._viewModel.getActiveFeatureId();
  const selectedVertexIdsSnapshot = this._viewModel.getSelectedVertexIds();
  const hasSelection = !!activeFeatureId || selectedVertexIdsSnapshot.size > 0;

  if (event.key === 'Escape') {
    ...
    } else if (hasSelection) {
      this._viewModel.clearSelection();
    }
  }
  ```
  キーボードショートカットの分岐が複雑化しているのに、観察可能な出力（`clearSelection` 呼び出し等）を自動で検証する仕組みが欠如している。
- 課題:
  1. DOM API の差し替えや `window` へのイベント登録をテスト環境で再現する土台がなく、実質的に統合テストを走らせる術がない。
  2. 表示モード・編集モード・ツール状態の組み合わせを網羅的に確認する作業が人手に依存しており、退行リスクが高い。
- 対応方針の例:
  1. `MapViewEventHandler` をキーボード入力専用のロジッククラスに切り出し、`handleKeyDown` に必要な依存（選択状態やモード）をインターフェース化する。
  2. もしくは JSDOM 上で `MapViewEventHandler` を初期化できるヘルパーを用意し、`window.dispatchEvent(new KeyboardEvent(...))` を使った統合テストを整備する。

- Facade の内部フィールドへ依存するテスト (`tests/presentation/viewModels.test.js`)
  ```js
  const worldRepository = {
    getWorld: vi.fn(async () => createWorld()),
    saveWorld: vi.fn(async () => {})
  };
  const editFeatureUseCase = { _worldRepository: worldRepository };
  ```
  `EditFeatureUseCase` の内部フィールド `_worldRepository` を直接 stub 化しており、Facade の実装詳細に縛られている。イベント通知や公開メソッドの戻り値で挙動を検証する作りにしないと、内部構造の変更でテストが壊れる。
- 該当テストケース: `tests/presentation/viewModels.test.js` の `it("loads world data and exposes only visible features")`

- Facade 内部の該当フィールド (`src/application/usecases/EditFeatureUseCase.js`)
  ```js
  constructor(worldRepository, geometryService, layerService, polygonEditService, idGenerationService) {
    this._worldRepository = worldRepository;
    ...
  }
  ```
  `_worldRepository` は内部状態として保持されているだけで、外部から直接差し替える契約は存在しない。テスト側が無理に触れると保守性が低下する。
- 該当実装箇所:
  - `src/presentation/view-models/MapViewModel.js` のコンストラクタ（依存を受け取って保持）
  - 同ファイルの `loadWorld`（`EditFeatureUseCase` の公開 API を利用する想定）
- 想定される副作用  
  - 実装側で `_worldRepository` を `#worldRepository` といったクラスフィールドに変更、または `EditFeatureUseCase` が内部で別のラッパーを保持するようになった場合、テストは即座に失敗する。  
  - `MapViewModel` の初期化ロジックが `editFeatureUseCase` のメソッドを直接呼び出すように変わると、`_worldRepository` を差し替えてもロード処理がスタブを通らなくなる。  
  - イベント駆動で `MapViewModel` が `worldRepository` とは非同期にやり取りする設計へ移行した際、テストが現在の同期フローを前提にしているとレースコンディションが露見しない。
- 見直し指針  
  - 世界データ読込の成功は `viewModel.getFeatures()` やイベント通知 (`eventBus.publish`) の結果で検証し、リポジトリ stub は `EditFeatureUseCase` の公開 API を通じて供給する。  
  - `MapViewModel` のテストでは `EditFeatureUseCase` 用のスタブに `addFeature` / `deleteFeature` 等のメソッドを用意し、`_worldRepository` には触れずに挙動を観察する。

## EditFeatureUseCase の実装詳細依存
- 委譲先を直接差し替えるテスト (`tests/application/editFeatureUseCase.test.js`)
  ```js
  const useCase = new EditFeatureUseCase(
    worldRepository,
    geometryService,
    layerService,
    polygonEditService,
    idService
  );

  useCase._addFeatureUseCase = { execute: vi.fn().mockResolvedValue({ id: "added" }) };
  useCase._updateFeatureUseCase = { execute: vi.fn().mockResolvedValue({ id: "feature-1", name: "Updated" }) };
  useCase._deleteFeatureUseCase = { execute: vi.fn().mockResolvedValue(undefined) };
  useCase._vertexEditUseCase = { moveVertices: vi.fn().mockResolvedValue({ updated: true }) };
  ```
  非公開フィールドに直接アクセスして委譲の呼び出し回数だけを確認している。Facade の内部構成が変わった場合に即座にテストが失敗する。
- 該当テストケース:
  - `tests/application/editFeatureUseCase.test.js` の `it("dispatches to AddFeatureUseCase for creation")`
  - 同ファイルの `it("forwards updates to UpdateFeatureUseCase")`
  - 同ファイルの `it("forwards deletions to DeleteFeatureUseCase")`
  - 同ファイルの `it("routes vertex movement through VertexEditUseCase")`

- Facade が依存を自前で生成している実装 (`src/application/usecases/EditFeatureUseCase.js`)
  ```js
  this._addFeatureUseCase = new AddFeatureUseCase(
    worldRepository, geometryService, layerService,
    this._generateIdFunc, this._processGeometryFunc, this._getVerticesFromIdsFunc
  );
  this._updateFeatureUseCase = new UpdateFeatureUseCase(
    worldRepository, geometryService, layerService,
    this._processGeometryFunc, this._getVerticesFromIdsFunc,
    polygonEditService
  );
  this._deleteFeatureUseCase = new DeleteFeatureUseCase(
    worldRepository,
    this._cleanupUnusedVerticesFunc
  );
  this._vertexEditUseCase = new VertexEditUseCase(
    worldRepository, geometryService,
    this._cleanupUnusedVerticesFunc,
    this._generateIdFunc, this._getOlderVertexIdFunc,
    layerService
  );
  ```
  委譲先を内部で new しているため、テストは公開 API を通じて結果を検証するか、依存差し替え用の仕組みを設計として用意しない限り保守性が担保できない。
- 該当実装箇所:
  - `src/application/usecases/EditFeatureUseCase.js` のコンストラクタ（委譲先 UseCase を new して内部保持）
  - 同ファイル内の各公開メソッド（`addFeature` / `updateFeature` / `deleteFeature` / `moveVertices`）が内部委譲へ接続
- 詳細な問題点  
  - `EditFeatureUseCase` はコンストラクタで `new Date().getTime()` を利用する `generateId` 委譲等を内部生成するため、テストが `_addFeatureUseCase` を差し替えても ID 発行や Geometry 処理の副作用が残存する可能性がある。  
  - 実装が将来、委譲先を `Map` に保持したり、ファクトリ関数に切り出した場合、テストは内部構造を直接再設定できず、委譲確認そのものが不可能になる。  
- 推奨される修正方針  
  1. `_addFeatureUseCase` など private フィールドにアクセスせず、`useCase.addFeature` 等の公開メソッドに対して「世界の変化」「返り値」「イベント発火」を検証する end-to-end 形式へ置き換える。  
  2. どうしても委譲を監視したい場合は、実装に「依存差し替え用のオプション引数」を公式に追加し、その契約をテストでも利用する。テストはその契約を通じてのみ差し替えを行い、 private フィールド名の直接参照を廃止する。
