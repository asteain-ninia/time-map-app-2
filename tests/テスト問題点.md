# テスト問題点メモ

## [未着手: 2026-03-03] 分割ツールの穴・飛び地・下位領域シナリオの統合テスト不足
- 移管元:
  - `現状.md` 重要度中-2 の残課題。
- 現状:
  - `tests/domain/polygonSplitService.test.js` には、穴を含む分割のドメイン単体テストがある。
  - `tests/application/splitPolygonUseCase.test.js` と `tests/presentation/` には、穴・飛び地・下位領域を含む分割シナリオの保存導線や Undo/Redo を検証する自動テストがない。
- 残課題:
  - 履歴アンカー付きの穴あり面を分割して保存できることを自動検証する。
  - 飛び地（複数の領土リング）を含む分割結果の履歴・Undo/Redo を自動検証する。
  - 下位領域を持つ面情報の分割制約を、UI と UseCase の両方で固定する自動テストを追加する。

## [未着手: 2026-02-27] 時間変化機能のE2E観点（タイムライン操作＋編集＋履歴復元）の手動確認手順の定例化
- 移管元:
  - `現状.md` 重要度高-3 の残作業。
- 残課題:
  - タイムライン操作→編集→履歴復元（Undo/Redo）を含む手動確認手順を定義する。
  - 実施頻度（例: 機能追加時、リリース前）を定めて運用化する。
  - 手動手順のうち自動化可能な項目を `tests/` 配下へ順次昇格する。

## [完了: 2026-02-14] MapViewEventHandler の競合ダイアログ操作の未検証
- `tests/presentation/mapViewEventHandler.test.js` を追加し、以下を自動検証済み。
  - 競合ダイアログ上で `Enter` 押下時に `data-action="confirm"` が発火すること。
  - 競合ダイアログ上で `Escape` 押下時に `data-action="cancel"` が発火すること。
  - 競合ダイアログ上の `mousedown` が地図操作に伝播しないこと。
- 該当実装:
  - `src/presentation/views/map/MapViewEventHandler.js:460-477`
  - `src/presentation/views/map/MapViewEventHandler.js:104`
- 残課題:
  - `window.dispatchEvent(new KeyboardEvent(...))` を含む実配線テストは未導入（`setupEventListeners` 経由の網羅は今後の課題）。
